# 05 — Reinforcement Learning จากศูนย์: ทำไม Tron1 ถึงต้องใช้ PPO

> โน้ตนี้อธิบาย RL ตั้งแต่ไม่มีพื้นฐานเลย จนถึงเข้าใจว่าทุกบรรทัดใน
> `agents/limx_rsl_rl_ppo_cfg.py` มาจากไหน — เขียนให้อ่านรวดเดียวจบตามลำดับ
> โดยแต่ละส่วนใช้สิ่งที่ส่วนก่อนหน้าสร้างไว้ ไม่มีการอ้างถึงสิ่งที่ยังไม่ได้
> อธิบาย คณิตศาสตร์เขียนเป็น LaTeX (เปิดดูใน VS Code preview หรือ Obsidian
> จะเห็นสมการเรนเดอร์)

## แผนที่ของโน้ตนี้

| ภาค | ตอบคำถามอะไร | หัวข้อ |
| --- | --- | --- |
| **1. โจทย์** | ปัญหาที่ต้องแก้คืออะไร เขียนให้เป็นคณิตศาสตร์ยังไง | MDP, Markov, step/episode, สมการเป้าหมาย, value function, reward/return/value |
| **2. คำตอบ** | สิ่งที่เราจะไปหา (policy) หน้าตาเป็นยังไง เชื่อมกับมอเตอร์ยังไง | neural network, μ/a/π(a\|s), การสุ่ม, ท่อแปลงค่าไปมอเตอร์, act vs act_inference |
| **3. ทางตัน** | ทำไมวิธี RL คลาสสิกใช้กับ Tron1 ไม่ได้ | tabular, Monte Carlo vs TD, bandits |
| **4. PPO** | PPO ประกอบขึ้นจากชิ้นส่วนอะไรบ้าง | actor-critic, encoder, advantage/GAE, clip/KL/entropy, กายวิภาคของ iteration |
| **5. สรุป** | เชื่อมทุกอย่างกลับเข้ากับ config จริง | ตาราง hyperparameter, สิ่งที่ยังไม่ได้พูดถึง |

**ถ้าอ่านครั้งแรก** — อ่านเรียงตามลำดับ ภาค 1 → 5 แต่ละภาคจบในตัวเอง
**ถ้ากลับมาหาอะไรเฉพาะจุด** — ใช้ตารางข้างบนหาภาคที่ใช่ก่อน

---
---

# ภาค 1 — โจทย์คืออะไร

ภาคนี้ยังไม่พูดถึงวิธีแก้เลย — เป้าหมายคือทำให้ปัญหา "หุ่นยนต์เดิน" กลายเป็น
โจทย์คณิตศาสตร์ที่เขียนเป็นสมการได้

---

## 1.1 ภาพรวม: RL กำลังแก้ปัญหาอะไร

ลองนึกภาพหุ่นยนต์ Tron1 ยืนอยู่บนพื้นราบ ทุก ๆ 0.02 วินาที (50 ครั้งต่อวินาที)
มันต้องตัดสินใจว่าจะสั่งมุมข้อต่อทั้ง 8 จุดเท่าไหร่ ถ้าสั่งผิด มันล้ม ถ้าสั่งถูก
มันเดินไปข้างหน้าได้ตามความเร็วที่สั่ง

ไม่มีใครเขียนสูตรคณิตศาสตร์ที่บอกว่า "ถ้าเอียงไปทางนี้ ให้สั่งมุมข้อต่อเท่านี้"
ได้ตรง ๆ เพราะระบบมันซับซ้อนเกินไป (แรงเสียดทาน, มวลกระจาย, momentum ของแต่ละขา
ฯลฯ) สิ่งที่เราทำได้แทนคือ **ให้หุ่นยนต์ลองเดินในซิมูเลเตอร์ แล้วให้คะแนน
(reward) กำกับ "ทุกครั้งที่ตัดสินใจ" ว่าการตัดสินใจครั้งนั้นดีหรือแย่** ทำซ้ำ
แบบนี้เป็นล้าน ๆ ครั้ง จากนั้นให้มันปรับพฤติกรรมเพื่อเก็บคะแนนสะสมให้มากที่สุด
— นี่คือแก่นของ reinforcement learning ทั้งหมด

> **หมายเหตุเรื่องหน่วย.** คำว่า "ทุกครั้งที่ตัดสินใจ" ข้างบนคือหน่วยที่เรียกว่า
> **step** — ใน Tron1 คือทุก ๆ 0.02 วินาที ดังนั้นการเล่นหนึ่งรอบ (episode) ที่
> ยาว 1000 step จะได้ตัวเลข reward ออกมา **1000 ตัว ไม่ใช่ตัวเดียว** และคะแนน
> ที่ใช้ปรับ weight จริง ๆ ก็ไม่ใช่ reward ดิบตัวนั้นตรง ๆ ด้วย แต่เป็นค่าที่
> แปลงมาอีกทีเรียกว่า **advantage** — สามคำนี้ (reward / return / advantage)
> ต่างกันชัดเจน แยกให้เห็นในหัวข้อ 1.7 และ 4.4

คำถามที่ RL ทุกวิธีพยายามตอบคือคำถามเดียวกัน: **"อยู่ในสถานการณ์นี้ ควรทำอะไร
เพื่อให้ได้ผลตอบแทนสะสมในระยะยาวมากที่สุด"** ต่างกันแค่ *วิธีคำนวณคำตอบ* ซึ่ง
ขึ้นอยู่กับว่าปัญหามีรูปร่างแบบไหน — และรูปร่างของปัญหานี่แหละที่ตัดวิธีเก่า ๆ
ออกไปทีละตัว (ภาค 3)

---

## 1.2 MDP — แบบฟอร์มสำหรับอธิบายปัญหา

ก่อนจะพูดถึงวิธีแก้ปัญหา ต้องมีภาษากลางเรียกชิ้นส่วนของปัญหาก่อน กรอบที่ใช้เรียก
เรียกว่า **Markov Decision Process (MDP)**

**MDP ไม่ใช่อัลกอริทึม ไม่ใช่วิธีแก้ปัญหา และไม่ใช่สิ่งที่ต้อง "ทำ" — มันคือ
แบบฟอร์มสำหรับอธิบายปัญหา** เท่านั้นเอง

#### เปรียบเทียบกับ URDF

URDF ไม่ใช่หุ่นยนต์ ไม่ใช่ controller และไม่ใช่ simulator — มันคือ **รูปแบบ
มาตรฐานสำหรับอธิบายว่าหุ่นยนต์ตัวหนึ่งมีหน้าตายังไง** (link มีอะไรบ้าง joint
ต่อกันยังไง มวลเท่าไหร่) พอเขียน URDF เสร็จ เครื่องมือต่าง ๆ (Gazebo, MoveIt,
rviz) ก็อ่านมันได้ เพราะทุกตัวรู้จักฟอร์แมตนี้

**MDP คือ URDF ของปัญหาการตัดสินใจ** — เป็นแบบฟอร์มที่บอกว่า "ถ้าจะอธิบาย
ปัญหาตัดสินใจแบบเป็นลำดับ ต้องระบุช่องเหล่านี้ให้ครบ"

#### แบบฟอร์ม MDP มี 5 ช่อง

| ช่อง | คำถามที่ต้องตอบ | Tron1 กรอกว่าอะไร |
| --- | --- | --- |
| **$S$** (states) | สถานการณ์ที่เป็นไปได้มีอะไรบ้าง | เวกเตอร์ 36 ค่าจากเซนเซอร์ — เป็นค่าต่อเนื่อง จึงมีได้ไม่จำกัด |
| **$A$** (actions) | ทำอะไรได้บ้าง | offset มุมข้อต่อ 8 ค่า (ต่อเนื่องเช่นกัน) |
| **$P$** (transition) | ทำ action นี้แล้วสถานการณ์จะเปลี่ยนไปยังไง | ฟิสิกส์ของ PhysX — เราไม่รู้สมการของมัน แต่ซิมูเลเตอร์ "รัน" มันให้ |
| **$R$** (reward) | ให้คะแนนยังไง | 20 reward terms รวมกัน ให้ทุก step |
| **$\gamma$** (discount) | อนาคตสำคัญเท่าปัจจุบันไหม | `0.99` |

กรอกครบ 5 ช่องเมื่อไหร่ → **ปัญหาของเรา "เป็น" MDP** และทันทีที่รู้ว่าเป็น MDP
ก็รู้ว่าเครื่องมือทั้งตระกูล RL (Q-learning, PPO, ฯลฯ) ใช้กับมันได้ เพราะ
เครื่องมือพวกนั้นถูกออกแบบมาให้กินฟอร์แมตนี้พอดี

นอกจาก 5 ช่องข้างบน ยังมีอีกคำที่ต้องรู้จัก — **ไม่ใช่ส่วนหนึ่งของแบบฟอร์ม
แต่เป็นสิ่งที่เรากำลังจะไปหา**:

| สัญลักษณ์ | ความหมาย | ตัวอย่างจริงใน Tron1 |
| --- | --- | --- |
| **policy** ($\pi$) | กฎที่แปลง state เป็น action — **คำตอบ**ของปัญหา ไม่ใช่ส่วนหนึ่งของโจทย์ | เครือข่าย neural network `42 → 512 → 256 → 128 → 8` (แกะละเอียดในภาค 2) |

#### ปัญหาที่ **ไม่ใช่** MDP — ดูจากตัวเปรียบเทียบจะชัดขึ้น

| ปัญหา | เป็น MDP ไหม | เหตุผล |
| --- | --- | --- |
| จำแนกรูปแมว/หมา | ✗ | ไม่มีลำดับเวลา — ทายรูปนี้เสร็จก็จบ ไม่ส่งผลต่อรูปถัดไป **ไม่มีช่อง $P$ ให้กรอก** |
| เครื่องสล็อต (multi-armed bandit) | ✗ (เกือบ) | มีช่อง $A$ กับ $R$ แต่ **ไม่มี $S$ ที่เปลี่ยนแปลง** — ดึงคันโยกแล้วโลกไม่เปลี่ยน เป็น MDP แบบพิการที่ตัด $S$ กับ $P$ ออก (ดูหัวข้อ 3.3) |
| แก้สมการหาคำตอบเดียว | ✗ | ไม่มีการตัดสินใจซ้ำ ๆ ตามเวลา |
| หมากรุก | ✓ | $S$ = กระดาน, $A$ = การเดินหมาก, $P$ = กติกา, $R$ = แพ้/ชนะ — ครบทุกช่อง (แต่เป็นแบบ **discrete** ต่างจาก Tron1) |
| **Tron1 เดิน** | ✓ | ครบทุกช่อง และเป็นแบบ **continuous** ทั้ง $S$ และ $A$ |

#### แล้วทำไมต้องตั้งชื่อให้มันด้วย

เพราะ **การรู้ว่าปัญหามีรูปร่างแบบไหน = รู้ว่าเครื่องมือไหนใช้ได้** เหมือนตอน
เห็นสมการแล้วบอกได้ว่า "นี่คือสมการกำลังสอง" — พอรู้ว่าเป็นกำลังสอง ก็รู้ทันที
ว่าใช้สูตรอะไรได้ โดยไม่ต้องคิดใหม่ตั้งแต่ต้น

พอเราบอกว่า *"Tron1 เป็น MDP ที่มี state และ action แบบต่อเนื่อง"* ประโยคเดียว
นี้ตัดตัวเลือกออกไปครึ่งหนึ่งทันที (tabular methods ใช้ไม่ได้ — หัวข้อ 3.1) และ
ชี้ไปยังตระกูลที่เหลือ (policy gradient → PPO) นี่คือประโยชน์ทั้งหมดของการมี
คำว่า MDP อยู่

---

## 1.3 Markov property — เงื่อนไขที่ซ่อนอยู่ในชื่อ

สังเกตว่าใน 5 ช่องข้างบน ไม่มีช่องไหนชื่อ "Markov" เลย แล้วทำไมตัว M ใน MDP
ถึงย่อมาจาก Markov?

คำตอบ: **Markov property ไม่ใช่ช่องให้กรอก แต่เป็น "เงื่อนไข" ที่แบบฟอร์มนี้
บังคับไว้เงียบ ๆ** — เป็นข้อสมมติที่ต้องเป็นจริง ถ้าจะเรียกปัญหานั้นว่า MDP
ได้อย่างถูกต้อง เปรียบเทียบกับ URDF อีกครั้ง: URDF มีเงื่อนไขเงียบ ๆ ว่า
โครงสร้างหุ่นยนต์ต้องเป็น tree ไม่มี loop — ถ้าหุ่นยนต์ของคุณมี closed-chain
คุณก็ยัง "เขียน URDF" ได้ แต่มันจะอธิบายหุ่นยนต์ได้ไม่ครบ และเครื่องมือที่
อ่านมันจะคำนวณผิด **Markov property คือเงื่อนไขทำนองเดียวกันของ MDP**

ชื่อ "Markov" มาจากเงื่อนไขข้อนี้:

$$\text{Markov property:}\quad P(s_{t+1} \mid s_t, a_t) = P(s_{t+1} \mid s_t, a_t, s_{t-1}, a_{t-1}, \dots, s_0, a_0)$$

**ความหมาย:** อนาคตขึ้นอยู่กับ **state ปัจจุบันเท่านั้น** — รู้ $s_t$ แล้ว
การรู้ประวัติทั้งหมดก่อนหน้านั้นเพิ่มเติม **ไม่ได้ช่วยอะไรอีกเลย** พูดอีกแบบ
คือ "state ปัจจุบันสรุปทุกอย่างที่จำเป็นต้องรู้เอาไว้หมดแล้ว"

#### ทำไม Tron1 ถึงไม่ Markov แท้

ลองดูตัวอย่างที่เป็นรูปธรรม สมมติมีหุ่นยนต์สองตัว อยู่ในสถานการณ์ที่
observation ทั้ง 36 ค่า **เหมือนกันเป๊ะทุกตัวเลข**:

| | หุ่นยนต์ A | หุ่นยนต์ B |
| --- | --- | --- |
| `joint_pos` (มุมข้อต่อ 8 ค่า) | เหมือนกันเป๊ะ | เหมือนกันเป๊ะ |
| `base_ang_vel` (ความเร็วเชิงมุม) | เหมือนกันเป๊ะ | เหมือนกันเป๊ะ |
| `proj_gravity` (ทิศแรงโน้มถ่วง) | เหมือนกันเป๊ะ | เหมือนกันเป๊ะ |
| **ความเร็วลำตัวจริง** (วัดไม่ได้!) | **1.0 m/s พุ่งไปข้างหน้า** | **0.0 m/s หยุดนิ่ง** |
| action ที่ควรทำ | ต้องเตรียมรับแรง/ก้าวขารับน้ำหนัก | ต้องออกแรงเริ่มก้าว |

สองตัวนี้ **ควรทำ action ที่ต่างกัน แต่ observation ที่ policy เห็นเหมือนกัน
ทุกประการ** — policy ไม่มีทางแยกแยะได้เลยว่าตอนนี้เจอเคสไหน นี่คือการ
**ละเมิด Markov property**: state ที่มีอยู่ไม่ครบพอที่จะกำหนดว่าอนาคตจะเป็น
อย่างไร ต้องอาศัยข้อมูลเพิ่มจากประวัติ

> **Remark.** สาเหตุที่ความเร็วลำตัวหายไปจาก observation ไม่ใช่ความผิดพลาด
> ในการออกแบบ แต่เป็นข้อจำกัดทางกายภาพ: หุ่นยนต์จริงมีแค่ IMU (วัดความเร่ง
> และความเร็วเชิงมุม) กับ joint encoder (วัดมุมข้อต่อ) — ไม่มีเซนเซอร์ตัวไหน
> วัด "ตอนนี้ลำตัวเคลื่อนที่ไปข้างหน้ากี่เมตรต่อวินาที" ได้โดยตรง (ไม่มี GPS
> ในอาคาร ไม่มีกล้อง motion capture ติดตัวไปด้วย) นี่คือชื่อเรียกทางการของ
> ปัญหาแบบนี้: **POMDP** (Partially Observable MDP) — MDP ที่มองเห็น state
> ได้แค่บางส่วน ทางแก้มาตรฐานของ POMDP คือ **เอาประวัติย้อนหลังมาช่วย** ซึ่ง
> เป็นหน้าที่ของ `obsHistory` + encoder — อธิบายเต็ม ๆ ในหัวข้อ 4.3 หลังจากที่
> รู้จัก neural network แล้ว

#### แล้วถ้าไม่ Markov จะพังตรงไหน

ไม่ใช่แค่ "ไม่สวยงามในทางทฤษฎี" แต่ **เครื่องมือ RL แทบทุกชิ้นที่จะสร้างต่อ
จากนี้ ตั้งอยู่บนเงื่อนไขนี้ทั้งหมด** ถ้ามันไม่จริง เครื่องมือก็คำนวณผิด:

| เครื่องมือ | ตั้งอยู่บน Markov ตรงไหน | ถ้าไม่ Markov จะพังยังไง |
| --- | --- | --- |
| **Value function** $V(s)$ (หัวข้อ 1.6) | นิยามบอกว่า "ค่าของ state $s$" — แปลว่าค่านี้ต้องขึ้นกับ $s$ **เท่านั้น** | หุ่นยนต์ A กับ B ข้างบนมี $s$ เดียวกัน แต่อนาคตต่างกันสิ้นเชิง → $V(s)$ ควรเป็นเลขอะไร? **ตอบไม่ได้** ฟังก์ชันนี้กลายเป็นสิ่งที่นิยามไม่ได้ |
| **สมการ Bellman** (สมการ 3) | เขียนว่า $V(s)=\mathbb{E}[r+\gamma V(s')]$ — ใช้ $s'$ ตัวเดียวสรุปอนาคตทั้งหมด | ถ้า $s'$ ไม่ได้สรุปอนาคตครบ สมการนี้ก็ไม่เป็นจริง → การคำนวณย้อนกลับทั้งสายผิดหมด |
| **Bootstrapping / TD / GAE** (หัวข้อ 3.2, 4.4) | อาศัยสมการ Bellman โดยตรง | ค่า $V(s_{t+1})$ ที่เอามาใช้ต่อ มี error ที่ไม่ได้เกิดจาก "ยังฝึกไม่พอ" แต่เกิดจากข้อมูลขาดโดยธรรมชาติ — ฝึกนานแค่ไหนก็ไม่หาย |
| **Policy** $\pi(a\|s)$ | สมมติว่า action ที่ดีที่สุดถูกกำหนดโดย $s$ เพียงอย่างเดียว | มี state ที่ action ที่ดีที่สุด "ขึ้นกับดวง" ว่าประวัติเป็นแบบไหน → policy ทำได้ดีที่สุดแค่การ**เฉลี่ย**ระหว่างสองเคส ซึ่งอาจแย่ทั้งคู่ |

พูดรวบยอด: **Markov property คือเสาที่ค้ำ RL ทั้งกอง** ถ้าเสานี้เอียง สิ่งที่
สร้างทับข้างบนก็เอียงตามหมด นี่คือเหตุผลที่ Tron1 ไม่ปล่อยให้ปัญหาไม่ Markov
ทิ้งไว้เฉย ๆ แต่ลงทุนสร้าง encoder ขึ้นมาซ่อมมันโดยเฉพาะ (หัวข้อ 4.3)

> **Remark.** จุดที่ควรเข้าใจให้ตรง: encoder **ไม่ได้ทำให้ปัญหากลับมา Markov
> แท้ ๆ 100%** — ความเร็วลำตัวที่ประมาณจากประวัติ 10 step ยังเป็นแค่ค่าประมาณ
> ที่มี error อยู่ดี สิ่งที่ทำได้คือทำให้ **"ใกล้เคียง" Markov มากพอ** ที่
> เครื่องมือทั้งกองข้างบนจะทำงานได้ดีในทางปฏิบัติ — RL เชิงประยุกต์เกือบ
> ทั้งหมดอยู่ในโหมดนี้ ไม่ใช่โหมด Markov สมบูรณ์แบบตามทฤษฎี

---

## 1.4 หน่วยเวลา: step กับ episode

ก่อนจะเขียนสมการเป้าหมายได้ ต้องมีหน่วยนับเวลาที่ชัดเจนก่อน สองคำนี้เป็นคนละ
ระดับกัน คล้ายกับ "เฟรมหนึ่งเฟรม" เทียบกับ "หนังทั้งเรื่อง":

| คำ | คือหน่วยอะไร | ใน Tron1 |
| --- | --- | --- |
| **step** (policy step) | การตัดสินใจ**ครั้งเดียว** — หนึ่งรอบของ: สังเกต state → policy คำนวณ action → สั่ง action → ฟิสิกส์ขยับ → ได้ reward | หนึ่ง step กินเวลา 0.02 วินาที (50Hz) — ภายในนั้นฟิสิกส์จริงขยับ 4 ครั้งย่อย (`decimation = 4` ที่ 200Hz) แต่ policy ตัดสินใจแค่ครั้งเดียวต่อ step |
| **episode** | การเล่น**ต่อเนื่องหลาย step** ตั้งแต่หุ่นยนต์เริ่มยืน จนจบ (ครบเวลา หรือล้ม) | สูงสุด 1000 steps ติดกัน (1000 × 0.02s = 20 วินาที) |

พูดง่าย ๆ **episode ประกอบด้วย step หลายอันเรียงต่อกัน** — step คือหน่วย
เวลาที่เล็กที่สุดที่ policy ได้ตัดสินใจ ส่วน episode คือ "หนึ่งรอบการเล่น
ทั้งหมด" ที่นับ step สะสมไปเรื่อย ๆ จนกว่าจะจบด้วยเหตุผลใดเหตุผลหนึ่ง:

- **`time_out`** — เดินครบ 1000 steps โดยไม่ล้ม → episode จบแบบดี (ที่
  `03-sf-task-anatomy.md` เรียกว่า truncation)
- **`base_contact`** — ลำตัวแตะพื้น (ล้ม) ก่อนครบเวลา → episode จบแบบล้มเหลว
  (termination)

ตัวอย่างการนับ step ในหนึ่ง episode:

```text
step 0   → state เริ่มต้น (ยืนตรง) → action → reward เล็กน้อย
step 1   → state ใหม่ → action → reward
step 2   → ...
   ⋮
step 847 → หุ่นยนต์เซ ล้ม → base_contact เกิดขึ้น → episode จบที่ step 847
```

(หรือถ้าไม่ล้ม จะเดินไปจนถึง step 999 แล้วจบด้วย `time_out`)

> **Remark.** จุดที่มักงงคือ แต่ละ environment (จากทั้ง 2048 ตัวที่รันขนาน
> กัน) อยู่ที่ step คนละจุดของ episode คนละอันได้พร้อมกัน — บางตัวเพิ่งเริ่ม
> step 5 บางตัวใกล้จบที่ step 998 บางตัวเพิ่งล้มแล้วรีเซ็ตกลับไป step 0 ใหม่
> ทั้งหมดนี้เกิดพร้อมกันในเวลาจริงเดียวกัน — ผลที่ตามมาของเรื่องนี้อยู่ใน
> หัวข้อ 4.6 (หน่วยของฝั่งอัลกอริทึม)

> **ยังมีหน่วยที่สามชื่อ `iteration`** ซึ่งเป็นหน่วยของ*การเรียนรู้* ไม่ใช่
> ของ*ปัญหา* — เก็บไว้พูดในหัวข้อ 4.6 เพราะต้องรู้จักอัลกอริทึมก่อนถึงจะเข้าใจ

---

## 1.5 สมการเป้าหมาย — เขียนโจทย์ให้เป็นคณิตศาสตร์

พูดเป็นภาษาธรรมดาก่อน: **เราอยากได้ policy ที่พอเอาไปรันหุ่นยนต์เต็ม episode
(1000 step) แล้ว บวกคะแนน reward ทุก step เข้าด้วยกัน จะได้ผลรวมสูงที่สุด
โดยเฉลี่ย** (พูดว่า "โดยเฉลี่ย" เพราะทั้งการสุ่ม action และการสุ่มเริ่มต้น
episode ทำให้แต่ละครั้งได้ผลรวมไม่เท่ากันเป๊ะ) แค่นั้นเอง — สมการด้านล่างเป็น
แค่การเขียนประโยคนี้ให้เป็นสัญลักษณ์:

| ส่วนของสมการ | อ่านว่า | ความหมาย |
| --- | --- | --- |
| $\sum_{t=0}^{T} \gamma^t r_t$ | ผลรวมของ reward ทุก step ตั้งแต่ $t=0$ ถึง $t=T$ (จบ episode) ถ่วงด้วย $\gamma^t$ | คะแนนสะสมทั้ง episode หนึ่งรอบ — ใน Tron1 คือผลรวม 1000 step |
| $\mathbb{E}[\cdot]$ | "ค่าคาดหวัง" (expectation) — ค่าเฉลี่ยถ้าเล่นซ้ำหลาย ๆ รอบ | เพราะ action สุ่มจาก Gaussian และ episode แต่ละรอบเจอสถานการณ์เริ่มต้นไม่เหมือนกัน ผลรวมคะแนนแต่ละรอบไม่เท่ากัน จึงพูดถึง "ค่าเฉลี่ยที่คาดว่าจะได้" แทนตัวเลขตายตัว |
| $\arg\max_{\pi}$ | "หา $\pi$ ที่ทำให้สิ่งข้างหลังมีค่ามากที่สุด" | นี่คือ "การฝึก" ทั้งหมด — ค้นหา policy ที่ดีที่สุด |
| $\pi^{*}$ | policy ที่ดีที่สุดที่หาได้ (มี `*` กำกับ ตามธรรมเนียมหมายถึง "คำตอบที่เหมาะสมที่สุด") | ในทางปฏิบัติเราไม่มีทางได้ตัวที่ดีที่สุดจริง ๆ 100% — `train.py` แค่พยายามไต่เข้าใกล้มันเรื่อย ๆ ตลอด `max_iterations 15000` รอบ |

$$\boxed{\;\pi^{*} = \arg\max_{\pi} \; \mathbb{E}\!\left[\sum_{t=0}^{T} \gamma^{t} r_t \right]\;} \tag{1}$$

**ความหมายรวม:** "ในบรรดา policy ที่เป็นไปได้ทั้งหมด ให้เลือกตัวที่พอเอาไปสั่ง
หุ่นยนต์แล้ว ผลรวมคะแนนเฉลี่ยทั้ง episode สูงที่สุด" — ทุกบรรทัดโค้ดของ
`train.py` ที่รันอยู่ 15000 iterations คือการพยายามไต่เข้าใกล้คำตอบของสมการนี้
ทีละก้าวเล็ก ๆ

$\gamma$ (gamma, discount factor) ใน $\gamma^t$ เป็นตัวเลข 0 ถึง 1 ที่บอกว่า
reward ในอนาคตมีค่าน้อยกว่า reward ตอนนี้แค่ไหน — เหมือนดอกเบี้ยเงินฝาก
กลับด้าน ใน Tron1 ใช้ `gamma = 0.99` แปลว่า reward ที่จะได้อีก 100 step
ข้างหน้า ยังมีน้ำหนักราว $0.99^{100} \approx 0.37$ เท่าของ reward วันนี้ —
โมเดลถูกบังคับให้มองการณ์ไกลพอสมควร แต่ไม่ไกลจนไม่สนใจ reward ระยะสั้นเลย

สมการ (1) นี้คือโจทย์ทั้งหมด ทุกอัลกอริทึมที่จะพูดถึงต่อไป — tabular, Monte
Carlo, PPO — ล้วนพยายามแก้สมการเดียวกันนี้ แค่ *วิธีประมาณคำตอบ* ต่างกัน

---

## 1.6 Value function กับสมการ Bellman

ก่อนจะหาว่า action ไหนดี มีสิ่งหนึ่งที่มีประโยชน์มากกว่านั้นอีก: รู้ว่า *สถานะ*
ปัจจุบันดีแค่ไหนโดยรวม (ไม่ต้องรู้ action ก่อน) เช่น หุ่นยนต์ที่กำลังยืนตรงนิ่ง ๆ
"ดีกว่า" หุ่นยนต์ที่เอียง 45 องศาใกล้ล้ม แม้จะยังไม่ได้ตัดสินใจอะไรเลยก็ตาม

ค่านี้เรียกว่า **value function** $V(s)$ — นิยามว่าเป็นผลรวม reward ที่คาดว่า
จะได้ *ถ้าเริ่มจาก state $s$ แล้วเดินตาม policy $\pi$ ไปเรื่อย ๆ*

$$V^{\pi}(s) = \mathbb{E}_{\pi}\!\left[\sum_{k=0}^{\infty} \gamma^{k} r_{t+k} \;\middle|\; s_t = s\right] \tag{2}$$

Value function เขียนใหม่แบบ **เวียนเกิด (recursive)** ได้ — เพราะ "ผลรวม reward
จากตอนนี้ไปตลอด" เท่ากับ "reward ก้าวเดียวข้างหน้า บวกกับ (ผลรวมจากก้าวถัดไป
คูณ discount)" นี่คือ **สมการ Bellman**:

| ขั้นตอน | เหตุผล |
| --- | --- |
| $V^{\pi}(s) = \mathbb{E}\left[\sum_{k=0}^{\infty}\gamma^k r_{t+k}\right]$ | นิยามตั้งต้น |
| $= \mathbb{E}\left[r_t + \gamma \sum_{k=0}^{\infty}\gamma^k r_{t+1+k}\right]$ | แยก reward แรกออกจากผลรวมที่เหลือ |
| $= \mathbb{E}\left[r_t + \gamma\, V^{\pi}(s_{t+1})\right]$ | ผลรวมที่เหลือคือนิยามของ $V^\pi$ ที่ state ถัดไปพอดี |

$$\boxed{\;V^{\pi}(s) = \mathbb{E}\big[r_t + \gamma\, V^{\pi}(s_{t+1})\big]\;} \tag{3}$$

**ความหมาย:** ค่าของสถานะปัจจุบัน = รางวัลทันทีที่ได้ + ค่าของสถานะถัดไป
(ถ่วงน้ำหนักด้วย discount) พูดง่าย ๆ คือ "ดีตอนนี้แค่ไหน ก็ต้องรวมด้วยว่ามันพา
ไปสู่อนาคตที่ดีแค่ไหนด้วย" สมการนี้เป็นรากฐานของ RL เกือบทุกสาขา และเป็นจุดที่
ทำให้เกิดแนวคิดสำคัญเรื่อง **bootstrapping** — การประมาณค่าปัจจุบัน โดยอ้างอิง
จากค่าประมาณของอนาคต แทนที่จะรอผลจริงทั้งหมด (จะสำคัญมากในหัวข้อ 3.2)

---

## 1.7 Reward, Return, Value — สามระดับของ "คะแนน" ที่มักสับสนกัน

สามคำนี้ฟังดูคล้ายกันแต่อยู่คนละระดับเวลากันชัดเจน:

| คำ | คือคะแนน ณ ตอนไหน | รู้ค่าได้ตอนไหน | ตัวอย่างใน Tron1 |
| --- | --- | --- | --- |
| **reward** ($r_t$) | คะแนน**หนึ่ง step เดียว** ที่ได้ทันทีหลังทำ action | รู้ทันที หลัง step นั้นจบ | ผลรวม 20 reward terms ที่ step นั้น เช่น ทรงตัวดีได้ `+0.8` เท้ากระแทกโดนหัก `-0.1` → รวม step นี้ `+0.7` |
| **return** ($G_t$) | ผลรวม reward ตั้งแต่ step นี้ **ไปจนจบ episode** (ถ่วงด้วย $\gamma$) — คือค่า**จริง**ที่เกิดขึ้นแล้ว ย้อนดูได้หลัง episode จบเท่านั้น | รู้ได้ก็ต่อเมื่อ episode จบแล้ว | ผลรวม reward ทั้ง 1000 step ที่เหลือของ episode นั้น เป็นตัวเลขจริง ไม่ใช่ค่าคาดเดา |
| **value** ($V(s)$) | **ค่าคาดหวัง** ของ return ถ้าเริ่มจาก state นี้ — เป็นค่า**ทำนาย/ประมาณ**ล่วงหน้า ไม่ใช่ค่าจริง | ทำนายได้ตั้งแต่ก่อนเริ่ม step ด้วยซ้ำ (มาจาก critic network — หัวข้อ 4.1) | critic มองท่ายืนตอนนี้ แล้วทำนายว่า "ท่านี้น่าจะเก็บคะแนนได้อีกราว 650 แต้มจนจบ episode" — ทั้งที่ episode ยังไม่จบ |

ความสัมพันธ์ระหว่างสามคำนี้เขียนเป็นสมการได้สั้น ๆ:

$$V(s) = \mathbb{E}\big[G_t \mid s_t = s\big] \tag{4}$$

**ความหมาย:** value คือ**ค่าเฉลี่ยที่คาดหวัง**ของ return ถ้าเล่นจาก state
นั้นซ้ำ ๆ หลายรอบ ส่วน return คือตัวเลข**จริง**ที่เกิดขึ้นในรอบใดรอบหนึ่ง
โดยเฉพาะ (ซึ่งจะแกว่งรอบ ๆ ค่า value เพราะมีความสุ่มของ action และของ
environment เข้ามา) พูดเป็นภาพ: reward คือ "คะแนนแต้มเดียวที่เพิ่งได้",
return คือ "ผลรวมคะแนนทั้งเกมที่จบไปแล้วจริง ๆ", value คือ "การเดาล่วงหน้าว่า
เกมจะจบด้วยคะแนนรวมเท่าไหร่ ตอนที่ยังเล่นอยู่"

> **Remark.** สมการ Bellman (3) จริง ๆ แล้วคือการเขียนนิยาม (4) ใหม่แบบ
> เวียนเกิด — "ค่าคาดหวังของ return จาก state นี้" เท่ากับ "reward ก้าวแรก
> บวกค่าคาดหวังของ return จาก state ถัดไป" นั่นเอง

**จบภาค 1 แล้วเรารู้อะไรบ้าง:** โจทย์คือสมการ (1) — หา policy ที่ทำให้ผลรวม
reward สูงสุด; เรามีภาษาเรียกชิ้นส่วน (MDP 5 ช่อง); รู้ว่าเงื่อนไข Markov
สำคัญแค่ไหนและ Tron1 ละเมิดมันตรงไหน; และมีเครื่องมือประเมินสถานะ (value
function + Bellman) **สิ่งที่ยังไม่รู้เลยคือ "policy" ที่ตามหาอยู่นั้น หน้าตา
เป็นยังไง** — ภาค 2 ตอบเรื่องนี้
---
---

# ภาค 2 — คำตอบหน้าตาเป็นยังไง: Policy

ภาค 1 บอกว่าเราตามหา $\pi$ อยู่ แต่ยังไม่เคยบอกว่ามันคืออะไร ภาคนี้แกะให้เห็น
ทุกชั้น ตั้งแต่ตัวเลขในโครงข่าย ไปจนถึงแรงบิดที่มอเตอร์

---

## 2.1 $\pi$ คืออะไรกันแน่ — และมี network กี่ก้อน

$\pi$ (อ่านว่า "pi" ไม่เกี่ยวกับเลข 3.14159) คืออะไร เพราะคำว่า "policy" ที่
พูดไปลอย ๆ ในภาค 1 ยังไม่มีรูปร่างชัด

**$\pi$ ไม่ใช่สูตรคณิตศาสตร์ที่มนุษย์เขียน และไม่ใช่ตาราง — มันคือก้อน neural
network ตัวเดียวกับที่พูดถึงใน `03-sf-task-anatomy.md`** ตัวมันเองมีรูปร่าง
ที่จับต้องได้เป็นชั้น ๆ ของตัวเลข (weight matrices) ขนาด `42 → 512 → 256 →
128 → 8` — รับ input เป็นเวกเตอร์ 42 ตัวเลข แล้วคายเวกเตอร์ 8 ตัวเลขออกมา
(offset มุมข้อต่อ)

ตอนเริ่มฝึก $\pi$ คือโครงข่ายที่มี weight สุ่ม — สั่งมุมข้อต่อมั่ว ๆ หุ่นยนต์
ล้มแทบทันที "การเรียนรู้" ที่พูดถึงตลอดทั้งโน้ตนี้ คือกระบวนการปรับตัวเลข
weight เหล่านั้นทีละนิด (ผ่าน gradient descent — หัวข้อ 2.2) จนกระทั่ง input
แบบไหนแล้วให้ output แบบไหนถึงจะทำให้หุ่นยนต์เดินได้

ทำไมถึงเขียน $\pi(a|s)$ (มีเครื่องหมาย `|`) แทนที่จะเขียน $\pi(s) = a$ ตรง ๆ:
เพราะ Tron1 (และ PPO ทั่วไป) ให้ policy คายออกมาไม่ใช่ action ตรง ๆ แต่คาย
**การแจกแจงความน่าจะเป็น (probability distribution)** ของ action — ในทาง
ปฏิบัติคือ neural network คายค่าเฉลี่ย $\mu$ ออกมา 8 ตัว (หนึ่งค่าต่อข้อต่อ)
แล้วนำ $\mu$ ไปเป็นค่ากลางของ Gaussian distribution (ระฆังคว่ำ) ที่มี
ส่วนเบี่ยงเบนมาตรฐาน $\sigma$ กำกับอยู่ด้วย ตอนฝึก action ที่สั่งออกไปคือการ
**สุ่ม**จากระฆังคว่ำนั้น ไม่ใช่ค่า $\mu$ ตรง ๆ

$$\pi(a \mid s) = \mathcal{N}\big(a\,;\, \mu_\theta(s),\, \sigma_\theta\big)$$

โดย $\theta$ (theta) คือชื่อเรียกรวม ๆ ของตัวเลข weight ทุกตัวในโครงข่าย —
เวลาบอกว่า "ฝึก policy" จริง ๆ แล้วหมายถึง "ปรับ $\theta$"

> **Remark เรื่องสัญลักษณ์.** สังเกตว่า $\mu_\theta(s)$ มี $(s)$ ต่อท้าย แต่
> $\sigma_\theta$ ไม่มี — **ตั้งใจเขียนต่างกัน** เพราะในโค้ดจริง
> ([actor_critic.py:118](../tron1-rl-isaaclab/rsl_rl/rsl_rl/modules/actor_critic.py#L118))
> เขียนว่า `self.logstd = nn.Parameter(torch.zeros(num_actions))` คือ $\sigma$
> เป็นพารามิเตอร์เดี่ยว ๆ 8 ตัว ที่ **ไม่ขึ้นกับ state เลย** (ฝึกได้ แต่ใช้ค่า
> เดียวกันหมดทุกสถานการณ์) ต่างจาก $\mu$ ที่คำนวณใหม่ทุก step จาก $s$
> ค่าเริ่มต้นคือ $\sigma = e^{0} = 1.0$

#### Tron1 มี neural network สองก้อน ไม่ใช่ก้อนเดียว

ระหว่างการฝึก Tron1 ไม่ได้ฝึก neural network แค่ก้อนเดียว แต่ฝึก **สองก้อน
คู่กัน** ที่มีหน้าที่ต่างกันสิ้นเชิง — ต้องรู้จักตรงนี้ก่อน เพราะหัวข้อต่อ ๆ ไป
จะอ้างถึงทั้งคู่ (เหตุผลเชิงอัลกอริทึมว่าทำไมต้องมีสองก้อน อยู่ในหัวข้อ 4.1)

| | **Actor** ($\pi$) | **Critic** ($V$) |
| --- | --- | --- |
| หน้าที่ | ตัดสินใจว่าจะสั่งมุมข้อต่อเท่าไหร่ — คือ policy ที่เราคุยกันมาตลอด | ให้คะแนนว่า "สถานการณ์ตอนนี้ดีแค่ไหน" ใช้เป็นครูสอน actor |
| output | 8 ตัวเลข (มุมข้อต่อ) | 1 ตัวเลข (คะแนนประเมิน = $V(s)$ จากหัวข้อ 1.6) |
| **เห็น input อะไรบ้าง** | **42 มิติ** — obs 36 ที่เซนเซอร์วัดได้ + command 3 + latent 3 จาก encoder | **227 มิติ** — ทุกอย่างที่ actor เห็น **บวก** ข้อมูลลับที่มีแค่ในซิม |
| ถูก deploy ลงหุ่นยนต์ไหม | **ใช่** — export เป็น `.onnx` ไปรันบนหุ่นยนต์จริง | **ไม่** — ถูกทิ้งทันทีที่ฝึกเสร็จ |

คำว่า **"เห็น"** ในตารางนี้ไม่ได้หมายถึงการมองด้วยกล้อง — หมายถึง **"เป็น
ตัวเลข input ที่ network ก้อนนั้นรับเข้าไปคำนวณได้"** เท่านั้น

**ข้อมูลลับ 227 มิติที่ critic เห็นแต่ actor ไม่เห็น** ประกอบด้วย (ตาม
`03-sf-task-anatomy.md`): `base_lin_vel` (ความเร็วลำตัวจริง — **ตัวที่หายไป
จนทำให้ไม่ Markov ในหัวข้อ 1.3 นั่นเอง**), แรงบิดและความเร่งของข้อต่อ,
ความเร็วและแรงสัมผัสของเท้า, และค่าพารามิเตอร์ที่ระบบสุ่มมาในรอบนั้น
(`robot_mass`, `robot_inertia`, `robot_joint_stiffness`,
`robot_joint_damping`, `robot_material_properties`)

**ทำไมให้ critic โกงดูเฉลยได้:** เพราะ critic ไม่เคยต้องไปทำงานบนหุ่นยนต์
จริง มันมีชีวิตอยู่แค่ในซิมูเลเตอร์ระหว่างการฝึกเท่านั้น ยิ่งมันประเมิน
สถานการณ์ได้แม่นยำ (จากข้อมูลที่ครบกว่า) ก็ยิ่งสอน actor ได้ดี ส่วน actor
ต้องถูกบังคับให้เก่งจากข้อมูลจำกัด เพราะมันคือตัวที่จะไปเจอโลกจริงที่ไม่มี
เฉลยให้ดู — เทคนิคนี้เรียกว่า **asymmetric actor-critic**

---

## 2.2 ข้างใน neural network — แกะให้เห็นทีละชั้น

หัวข้อก่อนบอกว่า $\pi$ "คือก้อน neural network" ลอย ๆ — ทีนี้แกะดูว่าข้างใน
ก้อนนั้นมีอะไรจริง ๆ

#### หนึ่ง node คืออะไร

Node (เรียกอีกชื่อว่า neuron) หนึ่งตัว ไม่ได้ทำอะไรซับซ้อน — มันรับตัวเลข
หลายตัวเข้ามา คูณแต่ละตัวด้วย "น้ำหนัก" (weight) ของตัวเอง บวกกันทั้งหมด
บวกค่าคงที่อีกตัว (bias) แล้วส่งผลรวมนั้นผ่านฟังก์ชันไม่เชิงเส้นตัวหนึ่ง
(activation function) ก่อนส่งออกไป:

$$\text{node output} = f\Big(\underbrace{w_1 x_1 + w_2 x_2 + \dots + w_n x_n + b}_{\text{ผลรวมถ่วงน้ำหนัก}}\Big) \tag{5}$$

| สัญลักษณ์ | ความหมาย |
| --- | --- |
| $x_1, x_2, \dots, x_n$ | ตัวเลข input ที่ node นี้รับเข้ามา (มาจาก node ชั้นก่อนหน้า หรือมาจาก observation ตรง ๆ ถ้าเป็นชั้นแรก) |
| $w_1, w_2, \dots, w_n$ | weight — ตัวเลขที่บอกว่า input แต่ละตัว "สำคัญแค่ไหน" ต่อ node นี้ **นี่คือสิ่งที่ถูกปรับระหว่างฝึก** |
| $b$ | bias — ค่าคงที่พิเศษ ปรับ threshold ว่า node นี้จะ "ตื่น" ง่ายหรือยาก |
| $f(\cdot)$ | activation function — ฟังก์ชันไม่เชิงเส้น (Tron1 ใช้ **ELU**) ที่บิดผลรวมให้ไม่เป็นเส้นตรง — ถ้าไม่มีขั้นนี้ ต่อให้ซ้อนกี่ชั้น เครือข่ายทั้งก้อนก็ยังทำได้แค่งานเชิงเส้นเท่านั้น |

หนึ่ง**ชั้น** (layer) คือกลุ่มของ node หลายตัวที่รับ input ชุดเดียวกัน แต่
แต่ละตัวมี weight ของตัวเองไม่เหมือนกัน คำว่า "512" ใน `42 → 512 → 256 →
128 → 8` แปลว่าชั้นนั้นมี node 512 ตัว โดยแต่ละตัวรับ input ทั้ง 42 ค่าเข้ามา
พร้อมกัน (คนละชุด weight) แล้วคาย output ออกมาคนละ 1 ค่า รวมเป็น 512 ค่า
ส่งต่อให้ชั้นถัดไป

> **Remark.** "เครือข่าย" (network) คือการเอา node หลายชั้นมาต่อกันเป็นสาย —
> output ของชั้นหนึ่งเป็น input ของชั้นถัดไป ไล่ไปเรื่อย ๆ นี่คือที่มาของคำว่า
> "deep" ใน deep learning: deep แปลว่ามีหลายชั้นซ้อนกัน ไม่ได้แปลว่าฉลาดลึกซึ้ง

#### ทำไมชั้นสุดท้ายถึงไม่มี activation — และมันควรมีไหม

ในโค้ดจริง ชั้นสุดท้ายของ actor คือ `nn.Linear(128, 8)` **ที่ไม่มี activation
ต่อท้าย** ต่างจากชั้นกลางที่มี ELU ทุกชั้น — ตั้งใจ ไม่ใช่ลืมใส่

เริ่มจากแยกให้ออกว่า activation มี **สองหน้าที่**:

| หน้าที่ | คำอธิบาย | จำเป็นตรงไหน |
| --- | --- | --- |
| **(ก) สร้างความไม่เชิงเส้น** | ทำให้การซ้อนหลายชั้นมีความหมาย | **ชั้นกลางเท่านั้น** |
| **(ข) จำกัดช่วงของ output** | เช่น `tanh` บีบเป็น $[-1,1]$, `sigmoid` บีบเป็น $[0,1]$ | ขึ้นกับว่าอยากได้ไหม |

หน้าที่ (ก) **สำคัญมากที่ชั้นกลาง** — ถ้าถอด ELU ออกจากชั้นกลางทั้งหมด เครือข่าย
`42→512→256→128→8` จะยุบเหลือการคูณเมทริกซ์ต่อกัน ซึ่งเท่ากับเมทริกซ์เดียว =
**ชั้นเดียว** ความลึกหายไปหมด (เพราะการคูณเมทริกซ์ต่อกันยังคงเป็นเมทริกซ์เดียว
เสมอ)

แต่ **ที่ชั้นสุดท้าย หน้าที่ (ก) ไม่มีความหมายเลย** เพราะไม่มีชั้นไหนต่อจากมัน
อีกแล้ว — จะไม่เชิงเส้นเทียบกับอะไร? เหตุผลเดียวที่จะใส่ activation ตรงนี้จึงมี
แค่หน้าที่ (ข) คืออยากบีบช่วงค่า **ซึ่งเราไม่อยาก** ด้วยสามเหตุผล:

**1. $\mu$ คือค่ากลางของ Gaussian ซึ่งทางคณิตศาสตร์เป็นจำนวนจริงอะไรก็ได้** —
ถ้าใส่ `tanh` ปิดท้าย $\mu$ จะถูกล็อกใน $[-1,1]$ ตลอดกาล ต่อให้ท่าเดินที่ดีที่สุด
ต้องการ action = 1.5 policy ก็ไปไม่ถึง เป็นการตัดความสามารถทิ้งโดยไม่จำเป็น

**2. tanh ทำให้ gradient ตาย (สำคัญกว่าข้อแรก)** — ที่ปลายทั้งสองข้างของ tanh
กราฟแบนราบ ความชันเข้าใกล้ 0:

```text
tanh(x)
  1 ┤        ────────────  ← ความชัน ≈ 0  (gradient ตาย)
    │      ╱
  0 ┤────╱                 ← ความชันดี
    │  ╱
 -1 ┤────────              ← ความชัน ≈ 0  (gradient ตาย)
```

ถ้า action ที่ดีที่สุดคือ 0.99 ต้องมี $x \approx 2.6$ ถ้าอยากได้ 0.999 ต้องมี
$x \approx 3.8$ — ยิ่งเข้าใกล้ขอบยิ่งต้องดัน $x$ ไปไกลแบบทวีคูณ ในขณะที่
gradient ที่ไหลย้อนกลับ (สมการ 6 ด้านล่าง) ยิ่งเล็กลงเรื่อย ๆ → weight แทบไม่
ถูกปรับ **ถ้าคำตอบที่ดีอยู่ใกล้ขอบ tanh จะกันไม่ให้ไปถึง**

**3. มีตัวกันพังอยู่แล้วที่อื่น** ไม่ต้องพึ่ง activation:

| ด่านกัน | อยู่ตรงไหน | ค่า |
| --- | --- | --- |
| `scale` | ตอนแปลงเป็นเรเดียน (หัวข้อ 2.5) | 0.25 (บีบ ±1 → ±0.25 rad) |
| `effort_limit` | ที่ actuator | 80 N·m |
| joint limits | ที่ตัวหุ่นยนต์ | `soft_joint_pos_limit_factor=0.9` |
| `pen_joint_pos_limits` | ที่ reward | น้ำหนัก −2.0 ลงโทษเมื่อเข้าใกล้ลิมิต |

**สรุป: ไม่ควรมี และการที่ไม่มีคือการออกแบบที่ถูกต้อง** เป็นมาตรฐานของ policy
gradient สำหรับ continuous control ทุกตัว สังเกตว่า **critic ก็ไม่มี activation
ที่ชั้นสุดท้ายเหมือนกัน** (`nn.Linear(hidden, 1)`) ด้วยเหตุผลเดียวกัน — $V(s)$
อาจเป็น 650 หรือ −200 ก็ได้ จะบีบให้อยู่ $[-1,1]$ ทำไม

> **ข้อยกเว้นที่มีอยู่จริง.** อัลกอริทึม **SAC** ใช้ tanh บีบ output จริง แต่
> ต้องเพิ่มพจน์แก้ค่า log-probability (Jacobian correction) เข้าไปในสมการเพื่อ
> ชดเชยการบิดเบือนของ distribution — เป็นการออกแบบที่ตั้งใจและมีคณิตศาสตร์
> รองรับ ไม่ใช่แค่ "แปะ tanh เฉย ๆ" ซึ่ง PPO ใน rsl_rl ไม่ได้ทำแบบนั้น

#### Input / Output ของ $\pi$ ทั้งก้อนคืออะไรกันแน่

ไม่ใช่แค่ node เดียว แต่ทั้งเครือข่าย `42 → 512 → 256 → 128 → 8` รวมกัน
ทำหน้าที่เป็นฟังก์ชันเดียว: รับเวกเตอร์ 42 ตัวเลขเข้า คายเวกเตอร์ 8 ตัวเลข
ออก

```text
input  (42 ตัวเลข)                                          output (8 ตัวเลข)
latent 3 ค่า        ─┐
  (จาก encoder)       │
observation 36 ค่า   ├─► [ชั้น 512] → [ชั้น 256] → [ชั้น 128] ─► μ (มุม offset
  (base_ang_vel,      │      node        node          node        8 ข้อต่อ)
   joint_pos, ...)    │    ทำสมการ(5)    ทำสมการ(5)    ทำสมการ(5)
commands 3 ค่า      ─┘      ×512           ×256          ×128
  (v_x, v_y, ω_z ที่สั่ง)
```

ตัวเลข 42 และ 8 **ไม่ใช่ทางเลือก** — 42 = **3 + 36 + 3** คือ latent จาก encoder
(**encoder คืออะไร อธิบายเต็ม ๆ ในหัวข้อ 4.3**) + observation ที่เซนเซอร์วัดได้
+ เป้าหมายความเร็วที่ผู้ใช้สั่ง ทั้งหมดถูกบังคับโดยโจทย์ — ประเด็นคือ
**input/output ถูกกำหนดโดยโจทย์ ไม่ใช่ถูกเลือกเอง** ส่วน 8 คือจำนวนข้อต่อ
จริงของ SF (2 ขา × 4 ข้อต่อ) เปลี่ยนไม่ได้เพราะเป็นจำนวนมอเตอร์จริงบน
หุ่นยนต์

> **ที่มาของเลข 42 (ตรวจจากโค้ดแล้ว).** โน้ต 03 นับ observation ได้ 36 เพราะ
> นับเฉพาะ group `policy` — แต่ actor รับ input **สามก้อน** ต่อกัน
> ([ppo.py:138](../tron1-rl-isaaclab/rsl_rl/rsl_rl/algorithm/ppo.py#L138)):
> `cat(encoder_out, obs, commands)` = **3 + 36 + 3 = 42** โดย `commands` คือ
> เป้าหมายความเร็ว $(v_x, v_y, \omega_z)$ ที่ผู้ใช้สั่ง เป็น observation group
> แยกชื่อ `"commands"` ไม่ได้อยู่ใน 36 — critic ก็ได้ `commands` ต่อท้ายเช่นกัน
> จึงเป็น 227 + 3 = 230 รายละเอียดเต็มอยู่ในโน้ต 06 หัวข้อ 1

#### Weight กับขนาด network ต่างกันยังไง — และทำไมขนาดถึง "ไม่ถูกปรับ" ระหว่างฝึก

ตัวเลข 512, 256, 128 (ชั้นตรงกลาง) ต่างจาก 42/8 ตรงที่ **เลือกเองได้** —
ตรงนี้ตอบคำถาม "ทำไมขนาดเท่านี้" ได้ตรง ๆ: **ไม่มีสูตรคณิตศาสตร์ที่คำนวณ
ขนาดที่ "ถูกต้อง" ออกมาได้** ขนาดชั้นกลางเป็นตัวเลขที่ผู้ออกแบบ (LimX) เลือก
จากประสบการณ์และการทดลอง (trial and error) — ไม่ใช่ค่าที่ derive มาจากทฤษฎี
เครือข่ายใหญ่กว่านี้ (เช่น 1024 → 512 → 256) ก็มีความสามารถจะเรียนรู้ได้
เหมือนกัน อาจจะดีกว่าเล็กน้อยหรือแย่กว่า (overfit, ฝึกช้าลง)

จุดที่ต้องแยกให้ออกคือ **สองสิ่งที่ดูคล้ายกันแต่เป็นคนละประเภท**:

| | Weight ($\theta$) | ขนาด/สถาปัตยกรรมของ network |
| --- | --- | --- |
| ใครเป็นคนกำหนด | gradient descent ปรับให้เอง ระหว่างฝึก | มนุษย์กำหนดไว้ **ก่อน** เริ่มฝึก แล้วคงที่ตลอด |
| เปลี่ยนแปลงได้ไหมระหว่างฝึก | เปลี่ยนทุก iteration (คือ "การเรียนรู้" ทั้งหมด) | ไม่เปลี่ยนเลยตลอดการฝึกหนึ่งรอบ |
| เรียกว่าอะไรในศัพท์ ML | **parameter** | **hyperparameter** |
| ตัวอย่างจำนวน | actor มี weight รวมหลักแสนตัว (42×512 + 512×256 + 256×128 + 128×8 บวก bias) | แค่ 3 ตัวเลข: 512, 256, 128 |

**นี่คือคำตอบของคำถาม "ขนาด network มีผลไหม เทียบกับการปรับ weight"** — จริง ๆ
แล้วขนาด network **มีผล** ต่อคุณภาพของ policy สุดท้ายเหมือนกัน (network เล็ก
เกินไปอาจเรียนรู้ท่าเดินซับซ้อนไม่ได้, ใหญ่เกินไปอาจฝึกช้าและ overfit) เพียงแต่
**ผลของมันถูกทดสอบด้วยวิธีคนละแบบ**: weight ถูกปรับ "ภายใน" การฝึกหนึ่งรอบด้วย
gradient descent อัตโนมัติ ส่วนขนาด network ถูกทดสอบด้วยการ**ฝึกซ้ำหลายรอบด้วย
ขนาดต่างกันแล้วเทียบผลลัพธ์** (เรียกว่า hyperparameter search) — เป็นการทดลอง
นอกลูปการฝึก ไม่ใช่ในลูป

> ตารางใน `notes/04-file-structure-vs-ros2.md` ระบุว่า "network architecture
> beyond hidden sizes" แก้ได้ที่ `agents/limx_rsl_rl_ppo_cfg.py` — เป็นค่าที่
> แก้ไขได้อิสระ ต่างจาก 42/8 ที่แก้ไม่ได้เพราะผูกกับโจทย์จริง

#### ตอนปรับ weight รู้ได้ไงว่าต้องปรับตัวไหน — หลักการของ gradient descent

คำถามนี้คือหัวใจของการฝึก neural network ทั้งหมด และคำตอบสั้น ๆ คือ:
**ไม่มีใครไป "เดา" ว่า weight ตัวไหนควรปรับ — มันคำนวณออกมาได้แม่นยำด้วย
แคลคูลัส (calculus) ทีละตัวพร้อมกันทั้งหมด**

ลองนึกภาพง่าย ๆ ก่อน (ยังไม่มี neural network เลย): สมมติเราอยู่บนภูเขา
หมอกลงจัดมองไม่เห็นอะไรเลย อยากเดินลงไปยังจุดต่ำสุด (หุบเขา) วิธีที่ทำได้
คือ **คลำพื้นรอบเท้าดูว่าทางไหนลาดลงชันที่สุด แล้วก้าวไปทางนั้นทีละก้าว**
ทำซ้ำไปเรื่อย ๆ จนถึงจุดต่ำสุด — นี่คือ **gradient descent** ทั้งหมดในหนึ่ง
ประโยค

ทีนี้แทนที่ "ความสูงของภูเขา" ด้วย **loss** (ในกรณี RL คือค่าลบของ objective
ในสมการ (1) — ยิ่ง policy แย่ ยิ่งค่าสูง) และแทนที่ "ตำแหน่งที่ยืนอยู่บนภูเขา"
ด้วย **ค่า weight ทุกตัวรวมกัน** ($\theta$) เราจะได้ภาพเดียวกันเป๊ะ:
**ภูเขานี้ไม่ใช่ 2 มิติ แต่มีจำนวนมิติเท่ากับจำนวน weight ทั้งหมด** (หลักแสน
มิติสำหรับ actor ของ Tron1) และเราต้องหาว่าจะ "ก้าว" ไปทิศทางไหนในพื้นที่
หลักแสนมิตินั้น เพื่อให้ loss ลดลงเร็วที่สุด

**Gradient** คือคำตอบของคำถามนั้น — มันคือเวกเตอร์ที่บอก **สำหรับ weight
แต่ละตัว ถ้าเพิ่มมันขึ้นนิดหนึ่ง loss จะเปลี่ยนไปเท่าไหร่** (คือ
อนุพันธ์ย่อย, partial derivative ของ loss เทียบกับ weight ตัวนั้น) —
คำนวณได้สำหรับ weight**ทุกตัวพร้อมกัน**ในครั้งเดียว ด้วยเทคนิคที่เรียกว่า
**backpropagation** (ไล่คำนวณ derivative ย้อนจาก output กลับไปยัง input
ทีละชั้น โดยอาศัย chain rule ของแคลคูลัส)

| สัญลักษณ์ | ความหมาย |
| --- | --- |
| $L(\theta)$ | loss — ตัวเลขเดียวที่บอกว่า policy (ที่มี weight ปัจจุบัน $\theta$) แย่แค่ไหน ยิ่งน้อยยิ่งดี |
| $\nabla_\theta L$ | gradient — เวกเตอร์ยาวเท่าจำนวน weight ทุกตัว บอกทิศทางที่ทำให้ $L$ *เพิ่มขึ้น* เร็วที่สุด |
| $\alpha$ | learning rate — ก้าวยาวแค่ไหนต่อครั้ง (คือค่า `lr` ในตาราง PPO hyperparameters) |

อัปเดต weight ทุกตัวพร้อมกันด้วยกฎเดียว:

$$\boxed{\;\theta \leftarrow \theta - \alpha \, \nabla_\theta L(\theta)\;} \tag{6}$$

**ความหมาย:** เดินสวนทางกับ gradient (เพราะ gradient ชี้ทางที่ loss
*เพิ่ม* เราจึงเดินย้อนกลับเพื่อให้ loss *ลด*) ระยะก้าวถูกคุมด้วย $\alpha$ —
weight ตัวที่ "มีผลต่อความแย่ของ policy มาก" จะมีค่า derivative (ในเวกเตอร์
$\nabla_\theta L$) ที่ขนาดใหญ่ จึงถูกปรับมากกว่า ส่วน weight ที่แทบไม่มีผล
จะมีค่า derivative ใกล้ศูนย์ แทบไม่ถูกแตะเลย — **นี่คือคำตอบของ "รู้ได้ไงว่า
ต้องปรับตัวไหน": ไม่ใช่การเลือก แต่เป็นตัวเลขที่คำนวณออกมาได้ตรง ๆ ว่า
weight ไหนควรปรับมากปรับน้อยแค่ไหน ในทุกรอบ**

> **Remark.** ในทางปฏิบัติ PyTorch (ที่ Tron1 ใช้) คำนวณ backpropagation
> ให้อัตโนมัติทั้งหมด — โค้ดใน `algorithm/ppo.py` มีแค่สามบรรทัดหลัก:
> `loss.backward()` (คำนวณ $\nabla_\theta L$ ทุกตัวด้วย backprop),
> `optimizer.step()` (ทำสมการ (6) จริง ๆ), และ `optimizer.zero_grad()`
> (ล้างค่า gradient เก่าก่อนรอบถัดไป) ไม่มีใครนั่งคำนวณ derivative ด้วยมือ
---

## 2.3 $\mu$, $a$, $\pi(a|s)$ — policy คายอะไรออกมากันแน่

สมการ $\pi(a \mid s) = \mathcal{N}(a; \mu_\theta(s), \sigma_\theta)$ ในหัวข้อ
2.1 อ่านแล้วมักตีความผิดเป็น *"network คายความน่าจะเป็นออกมา"* — **ไม่ใช่**
network คาย $\mu$ เท่านั้น ส่วน $\pi(a|s)$ ต้อง **คำนวณเพิ่ม** จาก $\mu$ กับ
$a$ อีกทีหนึ่ง

#### สามสิ่งที่ต่างกัน แต่ชื่อคล้ายจนสับสน

| | คืออะไร | ขนาด | ใครสร้าง | เอาไปทำอะไร |
| --- | --- | --- | --- | --- |
| $\mu$ | ค่ากลางที่ network คายออกมา | **8 ตัวเลข** | `self.actor(obs)` | เป็นศูนย์กลางของระฆังคว่ำ |
| $a$ | action ที่สุ่มได้ | **8 ตัวเลข** | `distribution.sample()` | **ส่งไปสั่งมอเตอร์** |
| $\pi(a\|s)$ | ความหนาแน่นความน่าจะเป็นของ $a$ นั้น | **1 ตัวเลข** | `get_actions_log_prob(a)` | **ส่งให้อัลกอริทึม PPO เท่านั้น** |

ดูที่เข่าซ้ายข้อเดียว สมมติ $\mu = 0.42$, $\sigma = 0.3$:

```text
ความหนาแน่น
   ▲
1.3┤        ╭─╮          ← ยอดระฆังอยู่ที่ μ = 0.42
   │       ╱   ╲
1.1┤      ╱  │  ╲╮       ← π(a|s) = 1.088  ณ จุด a = 0.61
   │     ╱   │   ╲│         (คือ "ความสูงของกราฟ" ตรงจุดนั้น)
0.5┤   ╱     │    ╲╲
   │ ╱       │      ╲╲
   └─────────┼───┼──────────►  a
            0.42 0.61
             μ    ↑
                  └── ตัวนี้แหละที่ส่งไปสั่งมอเตอร์
```

**คำถามที่เกิดขึ้นตามธรรมชาติ:** ถ้า $\pi(a|s)$ แปลว่า "ความน่าจะเป็นที่จะสั่ง
มอเตอร์ไปตำแหน่ง $a$" แล้วมันเอาไปทำอะไรกับมอเตอร์?

**คำตอบ: ไม่ทำอะไรเลย — ตัวเลขนี้ไม่เคยถูกส่งไปที่มอเตอร์** มันถูกคำนวณขึ้นมา
เพื่อให้อัลกอริทึมเรียนรู้ใช้อย่างเดียว **สองเส้นทางนี้แยกกันสนิท**:

```text
a = 0.61  ──────► ×0.25 ──► 0.1525 rad ──► PD ──► มอเตอร์   ← เส้นทางควบคุม

π(a|s) = 1.088 ──► เก็บใส่ buffer ──► ใช้ตอนคำนวณ loss       ← เส้นทางเรียนรู้
                                        (ไม่แตะมอเตอร์เลย)
```

นี่คือเหตุผลที่ตอน deploy (หัวข้อ 2.6) ทิ้ง $\pi(a|s)$ ได้เลยโดยไม่มีผลอะไร —
บนหุ่นจริงไม่ต้องเรียนรู้แล้ว

#### ความหนาแน่นเกิน 1 ได้ — เพราะสิ่งที่ต้องเท่ากับ 1 คือ *พื้นที่* ไม่ใช่ *ความสูง*

สังเกตว่า $\pi(a|s) = 1.088$ ซึ่ง **เกิน 1** — ความน่าจะเป็นจริง ๆ เกิน 1 ไม่ได้
แต่ตัวเลขนี้ไม่ใช่ความน่าจะเป็น มันคือ **probability density** กฎที่แท้จริงคือ:

$$\int_{-\infty}^{\infty} p(a)\, da = 1 \qquad \text{(พื้นที่ใต้กราฟทั้งหมด = 1)}$$

**ไม่มีกฎไหนบอกว่าความสูงต้อง $\le 1$** ความสูงยอดระฆังคือ
$\frac{1}{\sigma\sqrt{2\pi}}$ ซึ่งขึ้นกับ $\sigma$ อย่างเดียว:

| $\sigma$ | ความสูงยอด | รูปร่าง |
| --- | --- | --- |
| 1.0 | 0.399 | เตี้ย กว้าง |
| 0.3 | **1.330** | สูงขึ้น แคบลง ← ตัวอย่างข้างบน |
| 0.1 | 3.989 | สูงมาก แคบมาก |

**ยิ่งแคบยิ่งต้องสูง** เพื่อรักษาพื้นที่ให้เท่ากับ 1 เสมอ

> **เปรียบเทียบกับความหนาแน่นมวล.** น้ำมีความหนาแน่น 1000 kg/m³ — เลข 1000
> ไม่ได้แปลว่า "หนัก 1000 kg" ต้องเอาไป**คูณปริมาตร**ก่อนถึงได้มวลจริง
> probability density ก็เหมือนกันเป๊ะ:
>
> $$\text{ความน่าจะเป็น} = \text{ความหนาแน่น} \times \text{ความกว้างของช่วง}$$
>
> | คำถาม | คำตอบ |
> | --- | --- |
> | $P(a = 0.61$ เป๊ะ ๆ$)$ | **= 0** (ความกว้าง 0 → พื้นที่ 0) |
> | $P(0.60 < a < 0.62)$ | $\approx 1.088 \times 0.02 = 0.022$ คือ ~2.2% |
>
> เลข 1.088 จึงไม่ใช่ "โอกาส 108.8%" แต่เป็นความหนาแน่นต่อหนึ่งหน่วยของ $a$

**และ PPO ไม่สนใจเรื่องนี้เลย** เพราะ $\rho$ (หัวข้อ 4.5) เป็น*อัตราส่วน*ของ
ความหนาแน่นสองค่า — ค่าคงที่ normalization ตัดกันหมด เหลือแค่ "policy ใหม่ชอบ
action นี้มากกว่าเดิมกี่เท่า" ซึ่งเป็นตัวเลขไร้หน่วยที่มีความหมายชัดเจน

#### คำนวณ $\pi(a|s)$ ได้ยังไง — สูตร Gaussian PDF

$$\boxed{\;p(a) = \underbrace{\frac{1}{\sigma\sqrt{2\pi}}}_{\text{ความสูงยอด}} \times \underbrace{\exp\!\left(-\frac{(a-\mu)^2}{2\sigma^2}\right)}_{\text{ตัวลดทอน } 0 \to 1}\;} \tag{7}$$

**แยกเป็นสองส่วนแล้วเข้าใจง่ายขึ้นมาก:**

| ส่วน | ขึ้นกับอะไร | ทำหน้าที่ | ช่วงค่า |
| --- | --- | --- | --- |
| $\frac{1}{\sigma\sqrt{2\pi}}$ | **$\sigma$ อย่างเดียว** | กำหนดความสูงยอดระฆัง | เท่าไหร่ก็ได้ (จึงเกิน 1 ได้) |
| $\exp\!\left(-\frac{(a-\mu)^2}{2\sigma^2}\right)$ | ระยะห่างจาก $a$ ถึง $\mu$ | ลดทอนตามระยะห่าง | **0 ถึง 1 เสมอ** |

**ความหนาแน่น = ความสูงยอด × เปอร์เซ็นต์ที่เหลือเมื่อห่างจากยอด**

ไล่คำนวณด้วยตัวเลขจริง ($\mu = 0.42$, $\sigma = 0.3$, $a = 0.61$):

| ขั้น | คำนวณอะไร | ผลลัพธ์ | เหตุผล |
| --- | --- | --- | --- |
| 1 | $\sigma\sqrt{2\pi} = 0.3 \times 2.5066$ | $0.75199$ | ตัวหารของส่วนหน้า |
| 2 | $1 / 0.75199$ | $\mathbf{1.32981}$ | **ความสูงยอดระฆัง** |
| 3 | $a - \mu = 0.61 - 0.42$ | $0.19$ | ห่างจากยอดเท่าไหร่ |
| 4 | $(a-\mu)^2 = 0.19^2$ | $0.0361$ | ยกกำลังสอง (ซ้ายขวาเท่ากัน) |
| 5 | $2\sigma^2 = 2(0.09)$ | $0.18$ | ตัวหารที่ปรับตามความกว้างระฆัง |
| 6 | $-0.0361 / 0.18$ | $-0.20056$ | เลขชี้กำลัง |
| 7 | $\exp(-0.20056)$ | $\mathbf{0.81828}$ | **ตัวลดทอน — เหลือ 81.8% ของยอด** |
| 8 | $1.32981 \times 0.81828$ | $\mathbf{1.08815}$ | **คำตอบ** |

**ตัวลดทอนลดเร็วแค่ไหน** — วัดระยะเป็น "กี่เท่าของ $\sigma$" ($z = \frac{a-\mu}{\sigma}$)
จะได้ตารางที่ใช้ได้กับทุก Gaussian:

| ห่างจากยอด | $z$ | $\exp(-z^2/2)$ | เหลือกี่ % ของยอด |
| --- | --- | --- | --- |
| ตรงยอดพอดี | 0 | 1.000 | **100%** ← สูงสุด |
| กรณีข้างบน (0.19 = 0.633σ) | 0.633 | 0.818 | **81.8%** |
| 1σ | 1 | 0.607 | 60.7% |
| 2σ | 2 | 0.135 | 13.5% |
| 3σ | 3 | 0.011 | 1.1% |

**ในโค้ดคำนวณใน log space** — `distribution.log_prob(actions)` ของ PyTorch เอา
log ของสมการ (7) ทั้งก้อน:

$$\log p(a) = -\frac{(a-\mu)^2}{2\sigma^2} \;-\; \log\sigma \;-\; \log\sqrt{2\pi}$$

การคูณกลายเป็นการบวก และ $\exp$ หายไป — **เหลือแค่บวกลบกับยกกำลังสอง** เร็วและ
เสถียรกว่ามาก (ตรวจได้: $\log(1.08815) = 0.08448$)

> $\log p$ เป็นบวกได้ (0.08448 > 0) เพราะความหนาแน่นเกิน 1 — ถ้าความหนาแน่นต่ำ
> กว่า 1 ค่า log จะติดลบ ไม่ใช่ความผิดปกติอะไร

ลองเล่นเองได้ ลองเปลี่ยน $\sigma$ ดูจะเห็นว่ายิ่งเล็กยิ่งสูง (σ=0.1 → ยอด 3.99)
ซึ่งเป็นสิ่งที่เกิดขึ้นจริงระหว่างเทรนเมื่อ `Mean action noise std` ค่อย ๆ ลดลง:

```python
import math
def pdf(a, mu, sigma):
    return (1/(sigma*math.sqrt(2*math.pi))) * math.exp(-((a-mu)**2)/(2*sigma**2))

print(pdf(0.61, 0.42, 0.3))   # 1.08815
```

#### จริง ๆ แล้ว $\pi(a|s)$ เป็นผลคูณของ 8 ตัว

[actor_critic.py:163-164](../tron1-rl-isaaclab/rsl_rl/rsl_rl/modules/actor_critic.py#L163-L164):

```python
def get_actions_log_prob(self, actions):
    return self.distribution.log_prob(actions).sum(dim=-1)
                                                └── รวม 8 ข้อต่อ
```

`.sum(dim=-1)` ในปริภูมิ log = **การคูณ** ในปริภูมิปกติ:

$$\pi(\mathbf{a}|s) = \prod_{i=1}^{8} p_i(a_i|s) \qquad\Longleftrightarrow\qquad \log \pi(\mathbf{a}|s) = \sum_{i=1}^{8} \log p_i(a_i|s)$$

แปลว่าโมเดลถือว่า **ข้อต่อทั้ง 8 สุ่มอิสระจากกัน** — เป็นข้อสมมติที่ทำให้คำนวณ
ง่ายมาก แลกกับการที่ policy แสดง "ความสัมพันธ์ระหว่างข้อต่อ" ในการสุ่มไม่ได้
(เช่น บอกไม่ได้ว่า "ถ้าเข่าซ้ายเบี่ยงขึ้น ควรให้สะโพกซ้ายเบี่ยงลงด้วย")

#### "Policy คือ NN และฟังก์ชัน $\pi$ ใช่ไหม" — ใช่ทั้งคู่ แต่คำนี้ถูกใช้ปนกัน

นี่เป็นความหลวมของวงการ RL เอง ไม่ใช่ความเข้าใจผิดของผู้อ่าน:

| เขียนแบบ | หมายถึง | เป็นอะไร |
| --- | --- | --- |
| "ฝึก policy $\pi$" / $\pi_\theta$ | **ตัวกฎทั้งก้อน** = neural network + $\sigma$ + วิธีสุ่ม | วัตถุ/ระบบ |
| $\pi(a\|s)$ (มีวงเล็บ) | **ฟังก์ชันความหนาแน่น** รับ $a$ กับ $s$ คืนตัวเลขเดียว | ฟังก์ชันคณิตศาสตร์ |

สังเกตว่า $\pi(a|s)$ **รับ argument สองตัว** — ไม่ใช่ $\pi(s) = a$ มันไม่ได้
"แปลง state เป็น action" แต่ "ตอบว่าคู่ $(s, a)$ นี้ น่าจะเกิดขึ้นแค่ไหน"

```text
policy (ทั้งก้อน)  =  actor network  +  σ  +  กติกาการสุ่ม
                       └─► "อยู่ที่ s ควรทำอะไร" → ตอบด้วย μ แล้วสุ่ม → ได้ a

π(a|s) (ฟังก์ชัน)  =  "คู่ (s, a) นี้ น่าจะเกิดแค่ไหน" → ตอบเป็นตัวเลขเดียว
```

**อันแรกใช้ควบคุมหุ่นยนต์ อันที่สองใช้เรียนรู้เท่านั้น** — และทั้งคู่มาจาก
network ก้อนเดียวกัน

---

## 2.4 ทำไมต้องสุ่ม — และ explore/exploit ทำงานยังไง

#### สองเหตุผล คนละระดับกัน

**เหตุผลที่ 1: ถ้าไม่สุ่ม จะไม่มี "การลองผิดลองถูก" เลย**

สมมติใช้ $\mu$ ตรง ๆ ไม่สุ่ม — หุ่นยนต์อยู่ในสถานะ $s$ หนึ่ง network คาย
$\mu_3 = 0.42$ ให้เข่าซ้าย ก็จะสั่ง 0.42 **ทุกครั้งที่เจอสถานะนี้ ตลอดไป**

แล้วมันจะรู้ได้ยังไงว่า 0.55 อาจดีกว่า? **ไม่มีทางรู้ เพราะไม่เคยลอง** ข้อมูล
ที่เก็บมาจะมีแต่ "ทำ 0.42 แล้วได้ reward เท่านี้" ซ้ำ ๆ ไม่มีอะไรให้เปรียบเทียบ
gradient ก็ไม่รู้ว่าควรดัน $\mu$ ขึ้นหรือลง

พอใส่ความสุ่มเข้าไป ข้อมูลที่ได้กลายเป็นแบบนี้:

| ครั้งที่ | $\mu$ (network คาย) | สุ่มได้ $a$ | reward |
| --- | --- | --- | --- |
| 1 | 0.42 | 0.61 | 0.85 ← ดี |
| 2 | 0.42 | 0.15 | 0.40 ← แย่ |
| 3 | 0.42 | 0.78 | 0.91 ← ดีมาก |
| 4 | 0.42 | −0.10 | 0.22 ← แย่มาก |

**ตอนนี้มีข้อมูลให้เปรียบเทียบแล้ว** — เห็นชัดว่ายิ่งค่าสูงยิ่งดี gradient จึง
คำนวณได้ว่าควรดัน $\mu$ จาก 0.42 ขึ้นไปทาง 0.7 นี่คือ "การลองผิดลองถูก" ที่
พูดถึงตั้งแต่หัวข้อ 1.1 — **ความสุ่มคือกลไกที่ทำให้เกิดการลองจริง ๆ**

**เหตุผลที่ 2: คณิตศาสตร์ของ PPO บังคับให้ต้องมี**

สมการ (12) ที่จะเจอในหัวข้อ 4.5 ใช้อัตราส่วนความน่าจะเป็น:

$$\rho_t = \frac{\pi_{new}(a_t \mid s_t)}{\pi_{old}(a_t \mid s_t)}$$

ตัวเศษและตัวส่วนคือ**ความน่าจะเป็น** — ถ้า policy ไม่สุ่ม มันจะให้ความน่าจะเป็น
1 กับ action เดียวและ 0 กับที่เหลือ อัตราส่วนกลายเป็น $1/1$ หรือ $0/0$ ตลอด
→ **สมการทั้งอันใช้ไม่ได้** PPO ต้องการ distribution จริง ๆ เพื่อวัดว่า policy
เปลี่ยนไปมากแค่ไหน

#### การสุ่มทำยังไงในโค้ด

[actor_critic.py:155-161](../tron1-rl-isaaclab/rsl_rl/rsl_rl/modules/actor_critic.py#L155-L161):

```python
def update_distribution(self, observations):
    mean = self.actor(observations)                                   # ← μ จาก network
    self.distribution = Normal(mean, mean * 0.0 + torch.exp(self.logstd))

def act(self, observations, **kwargs):
    self.update_distribution(observations)
    return self.distribution.sample()                                 # ← สุ่มตรงนี้
```

> `mean * 0.0 +` เป็นแค่ trick ให้ $\sigma$ ขยายขนาดตรงกับ batch เฉย ๆ ไม่มี
> ความหมายทางคณิตศาสตร์ (`mean * 0.0` ได้ศูนย์ทั้งหมด แล้วบวก $\sigma$ เข้าไป)

**ข้างใน `.sample()` ทำอะไร** — กลไกเรียบง่ายมาก: สุ่มจากระฆังมาตรฐานก่อน แล้ว
ยืดและเลื่อน

$$\boxed{\;a = \mu + \sigma \cdot \varepsilon, \qquad \varepsilon \sim \mathcal{N}(0, 1)\;} \tag{8}$$

| ขั้น | ทำอะไร | ตัวอย่าง |
| --- | --- | --- |
| 1 | สุ่ม $\varepsilon$ จากระฆังมาตรฐาน ($\mu=0$, $\sigma=1$) ด้วย RNG | $\varepsilon = 0.633$ |
| 2 | คูณด้วย $\sigma$ เพื่อ**ยืดความกว้าง** | $0.3 \times 0.633 = 0.19$ |
| 3 | บวก $\mu$ เพื่อ**เลื่อนไปที่ศูนย์กลางที่ถูกต้อง** | $0.42 + 0.19 = \mathbf{0.61}$ |

ระฆังมาตรฐาน $\mathcal{N}(0,1)$ เป็นตัวเดียวที่ต้องสุ่มจริง ๆ ที่เหลือเป็นแค่
การคูณและบวก — และการสุ่มจาก $\mathcal{N}(0,1)$ เป็นอัลกอริทึมมาตรฐานที่ RNG
ทำได้ (Box–Muller transform หรือคล้ายกัน)

ทำแบบนี้ **8 ครั้งพร้อมกัน** (ทีละข้อต่อ ด้วย $\varepsilon$ คนละตัว) และทำ
**2048 ชุดพร้อมกัน** (ทีละ env) เป็น tensor operation ครั้งเดียวบน GPU

> `.sample()` ทำงานภายใต้ `torch.no_grad()` — ตัวการสุ่มไม่มี gradient ไหลผ่าน
> gradient ไปเข้าที่ $\mu$ ผ่านทาง $\pi(a|s)$ ในสมการ loss แทน (หัวข้อ 4.5)

#### Explore กับ Exploit ไม่ใช่ "สองโหมด" ที่สลับกัน

ตำรา RL ส่วนใหญ่อธิบาย explore/exploit ด้วย **ε-greedy** (ของ Q-learning) ซึ่ง
เป็นการสลับโหมดจริง ๆ:

```text
ε-greedy — สองโหมดแยกกันชัดเจน:
   สุ่มเลข r
   ถ้า r < 0.1  →  explore: เลือก action มั่ว ๆ ไปเลย       ← โหมด A
   ถ้า r ≥ 0.1  →  exploit: เลือก action ที่ดีที่สุดที่รู้   ← โหมด B
```

**แต่ PPO ที่ใช้ Gaussian policy ไม่ได้ทำแบบนั้นเลย** — มันผสมทั้งสองอย่างเข้า
ด้วยกัน **ในทุก ๆ action**:

$$a = \underbrace{\mu}_{\text{exploit: ความรู้ที่ดีที่สุดตอนนี้}} + \underbrace{\text{noise}(\sigma)}_{\text{explore: ลองเบี่ยงจากที่รู้นิดหน่อย}}$$

| | ε-greedy | PPO Gaussian |
| --- | --- | --- |
| explore คือ | เลือก action มั่วทั้งตัว | เบี่ยงจาก $\mu$ เล็กน้อย |
| exploit คือ | เลือก action ที่ดีที่สุด | ค่า $\mu$ ที่เป็นแกนกลาง |
| สลับโหมดไหม | สลับ (โหมด A หรือ B) | **ไม่สลับ — ทำพร้อมกันทุก step** |
| ปรับความ "กล้าลอง" ยังไง | ลด ε ลงเรื่อย ๆ ตามตาราง | $\sigma$ ค่อย ๆ เล็กลงเอง จากการฝึก |

**ไม่มีวินาทีไหนที่หุ่นยนต์ "ทำมั่ว ๆ ล้วน ๆ"** — มันทำสิ่งที่ตัวเองคิดว่าดี
ที่สุด ($\mu$) แล้วเบี่ยงออกไปนิดหน่อยเพื่อดูว่าเบี่ยงแล้วดีขึ้นหรือแย่ลง

#### ผลที่ตามมา: เห็นหุ่นค่อย ๆ ตั้งได้ระหว่างเทรน ไม่ต้องรอจบ

คำถามที่เกิดขึ้นตามธรรมชาติคือ "ถ้าตอนเทรนสุ่มตลอด แล้วทำไมถึงเห็นหุ่นค่อย ๆ
เดินได้ ไม่ใช่มั่วไปจนจบ?"

คำตอบตามมาจากสมการข้างบนโดยตรง: **เพราะ $a = \mu + \text{noise}$ และ $\mu$
เก่งขึ้นเรื่อย ๆ ตลอดเวลา** — noise ไม่ได้ทำให้พฤติกรรมมั่ว มันแค่ทำให้ "เก่ง
แบบสั่น ๆ" รอบ ๆ ความเก่งปัจจุบัน

| ช่วงการเทรน | $\mu$ เป็นยังไง | $\sigma$ | สิ่งที่เห็นในจอ |
| --- | --- | --- | --- |
| iteration 0 | weight สุ่ม → คายค่ามั่ว | ~1.0 | ล้มทันทีทุกตัว กระตุกไปมา |
| ~100 | เริ่มรู้ว่าต้องยันพื้น | ↓ | ยืนได้แป๊บนึงแล้วล้ม |
| ~1,000 | ยืนได้ เริ่มก้าว | ↓ | เดินโซเซ ล้มบ้าง |
| ~5,000 | เดินได้ | ↓ | เดินค่อนข้างนิ่ง สั่นเล็กน้อย |
| ~15,000 | เดินดี | ต่ำ | เดินนิ่ง |

**สองอย่างดีขึ้นพร้อมกัน:** (1) $\mu$ ดีขึ้นจาก gradient descent ทุก iteration
— ตัวหลัก และ (2) $\sigma$ เล็กลง เพราะพอ policy เก่งแล้ว การสุ่มกว้าง ๆ ทำให้
reward ต่ำลง gradient จึงบีบ $\sigma$ ลง (มี `entropy_coef=0.01` คอยต้านไม่ให้
ลดเร็วเกินไป — หัวข้อ 4.5)

> **ดูของจริงได้ตอนเทรน.** $\sigma$ ไม่ใช่เรื่องนามธรรม — `train.py` พิมพ์ค่านี้
> ทุก iteration ในชื่อ `Mean action noise std`
> ([on_policy_runner.py:324](../tron1-rl-isaaclab/rsl_rl/rsl_rl/runner/on_policy_runner.py#L324))
> และบันทึกลง tensorboard ที่ `Policy/mean_noise_std`
> ([on_policy_runner.py:284](../tron1-rl-isaaclab/rsl_rl/rsl_rl/runner/on_policy_runner.py#L284))
> เปิดกราฟนี้ตอนรัน `exp_001` จะเห็นมันเริ่มที่ ~1.0 แล้วค่อย ๆ ลดลง คู่ไปกับ
> กราฟ reward ที่สูงขึ้น — **เป็นหลักฐานตรง ๆ ว่า explore ค่อย ๆ ลดลงระหว่างที่
> exploit ค่อย ๆ ดีขึ้น โดยไม่มีการสลับโหมดที่จุดไหนเลย**

---

## 2.5 จาก $\mu$ ไปถึงมอเตอร์ — ท่อแปลงค่าทั้ง 5 ขั้น

จนถึงตอนนี้เรารู้แล้วว่า network คาย $\mu$ ออกมา 8 ตัว แล้วสุ่มได้ $a$ — แต่
$a$ เป็นแค่ตัวเลขไร้หน่วย มันกลายเป็นคำสั่งที่มอเตอร์เข้าใจได้ยังไง

```text
[1] network คาย μ (8 floats)      →  เช่น เข่าซ้าย μ = 0.42   ← ไม่มีหน่วย
         │
         ▼
[2] สุ่มจาก N(μ, σ)                →  a = 0.61                ← ยังไม่มีหน่วย
         │                              (ตอน deploy ข้ามขั้นนี้ ใช้ a = μ)
         ▼
[3] คูณ scale = 0.25               →  0.61 × 0.25 = 0.1525
         │
         ▼
[4] บวก default joint position     →  0.1525 + 0.0 = 0.1525   ← เป็นเรเดียนแล้ว
         │
         ▼
[5] PD controller ใน PhysX         →  τ = 45·(0.1525 − q) − 1.5·q̇
         │
         ▼
      มอเตอร์ขยับ
```

ค่าจริงทั้งหมดมาจาก
[limx_base_env_cfg.py:113-120](../tron1-rl-isaaclab/exts/bipedal_locomotion/bipedal_locomotion/tasks/locomotion/cfg/SF/limx_base_env_cfg.py#L113-L120):

```python
joint_pos = mdp.JointPositionActionCfg(
    asset_name="robot",
    joint_names=[".*"],
    scale=0.25,              # ← ขั้น [3]
    use_default_offset=True, # ← ขั้น [4]
)
```

และค่า PD gain จาก
[solefoot_cfg.py:39-62](../tron1-rl-isaaclab/exts/bipedal_locomotion/bipedal_locomotion/assets/config/solefoot_cfg.py#L39-L62):

| ข้อต่อ | `stiffness` ($k_p$) | `damping` ($k_d$) | `effort_limit` |
| --- | --- | --- | --- |
| legs (abad, hip, knee) | 45.0 | 1.5 | 80.0 N·m |
| ankles | 45.0 | 0.8 | 80.0 N·m |

> **หมายเหตุ.** ค่า default joint position ของ Tron1 SF คือ **0.0 ทุกข้อ**
> (`joint_pos={".*_Joint": 0.0}`) ทำให้ขั้น [4] ในตัวอย่างนี้ดูเหมือนไม่ทำอะไร
> — แต่ในหุ่นยนต์ที่ default pose ไม่ใช่ศูนย์ ขั้นนี้สำคัญมาก

#### ทำไมต้องคูณ `scale = 0.25`

network คายค่าออกมาในย่านราว ๆ $\pm 1$ ถึง $\pm 2$ (เพราะ weight เริ่มต้นสุ่ม
เล็ก ๆ และ $\sigma$ เริ่มที่ 1.0) แต่มุมข้อต่อที่ควรขยับได้ใน **หนึ่ง step
(0.02 วินาที)** อยู่ที่ราว ๆ ไม่กี่องศาเท่านั้น:

| | ไม่คูณ scale | คูณ scale 0.25 |
| --- | --- | --- |
| $a = 1.0$ | 1.0 rad = **57 องศา** ใน 0.02 วินาที | 0.25 rad = **14 องศา** |
| $a = 2.0$ | 2.0 rad = **115 องศา** ใน 0.02 วินาที | 0.5 rad = **29 องศา** |

57 องศาใน 1/50 วินาทีคือการกระตุกรุนแรงมาก — PD จะสร้างแรงบิดมหาศาลจนชนลิมิต
80 N·m ทันที หุ่นยนต์กระเด็นล้มทุกครั้ง และ **ฝึกไม่ได้เลยเพราะทุก action พัง
เท่ากันหมด** ไม่มีสัญญาณให้เรียนรู้

มองอีกมุมคือ **การแบ่งงาน**: ถ้าไม่มี scale network ต้องเรียนรู้เองว่า "ต้องคาย
เลขเล็ก ๆ ประมาณ 0.1 นะ" ซึ่งทำได้ แต่เสียเวลาฝึกเพิ่มเพื่อบีบ weight ให้เล็กลง
การมี scale เท่ากับบอก network ล่วงหน้าว่า "คายเลขในย่าน ±1 ตามธรรมชาติได้เลย
เดี๋ยวแปลงหน่วยให้เอง" — network จึงโฟกัสที่การเรียนรู้ *รูปแบบ* การเดิน ไม่ต้อง
เสียเวลาเรียนรู้ *สเกลตัวเลข* (`0.25` เป็น hyperparameter เหมือน 512/256/128
และเป็นค่ามาตรฐานที่ใช้กันแพร่หลายในงาน legged locomotion)

#### ตัวเลขไร้หน่วย "กลายเป็นเรเดียน" ได้ยังไง

คำตอบที่อาจฟังดูแปลก: **ไม่มีการแปลงหน่วยเกิดขึ้นเลย — มันเป็นเรื่องของการ
ตีความล้วน ๆ**

เปรียบเทียบกับการอ่านเซนเซอร์: ADC ให้ตัวเลข `3.2` ออกมา ตัวเลขนี้ไม่รู้ว่า
ตัวเองคืออะไร แล้วเราเขียนโค้ด `float temp_celsius = adc_value * 25.0;`
**การคูณ 25 ไม่ได้แปลงอะไรในเชิงกายภาพ** — มันเป็น*เรา*ที่ตัดสินใจว่า "เลขจาก
ADC คูณ 25 แล้วจะเรียกว่าองศาเซลเซียส" แล้วเอาไปใส่ตัวแปรที่ชื่อแบบนั้น
**80.0 กลายเป็นองศาเซลเซียสเพราะปลายทางที่มันถูกส่งไป ไม่ใช่เพราะการคูณ**

ใน Tron1 ก็เหมือนกันเป๊ะ ไล่ตามโค้ดจริง:

| บรรทัด | โค้ด | ค่า | สถานะของตัวเลข |
| --- | --- | --- | --- |
| 1 | `actions = policy(obs)` | 0.61 | เลขเปล่า ไม่มีหน่วย |
| 2 | `processed = raw * scale + offset` ([joint_actions.py:134](../IsaacLab/source/isaaclab/isaaclab/envs/mdp/actions/joint_actions.py#L134)) | 0.1525 | **ยังเป็นเลขเปล่า** — Python ไม่รู้จักคำว่าเรเดียน |
| 3 | `set_joint_position_target(processed)` ([joint_actions.py:160](../IsaacLab/source/isaaclab/isaaclab/envs/mdp/actions/joint_actions.py#L160)) | 0.1525 | **← ตรงนี้กลายเป็นเรเดียน** |
| 4 | PhysX: $\tau = 45(0.1525 - q) - 1.5\dot q$ | — | ฟิสิกส์ใช้งานจริง |

**ฟังก์ชัน `set_joint_position_target()` ถูกนิยามโดย Isaac Lab/PhysX ว่า "ช่อง
นี้รับค่าเป็นเรเดียน"** พอส่ง 0.1525 เข้าไปในช่องนั้น มันก็**เป็น** 0.1525
เรเดียน (≈ 8.7 องศา) โดยปริยาย — **เลขกลายเป็นเรเดียนเพราะ "ช่อง" ที่มันถูก
เสียบเข้าไป ไม่ใช่เพราะมีสูตรแปลงพิเศษ**

> **จุดที่ลึกที่สุด: network ไม่เคยรู้จักคำว่า "เรเดียน" เลย.**
> ลองคิดดู — ถ้าแก้ `scale` จาก 0.25 เป็น 0.5 แล้วฝึกใหม่ จะเกิดอะไรขึ้น?
> คำตอบคือ **network จะเรียนรู้ที่จะคายเลขเล็กลงครึ่งหนึ่งเอง** (เดิมคาย 0.42
> จะกลายเป็น ~0.21) เพราะสุดท้ายมุมที่ทำให้เดินได้ดีคือค่าเดิม
>
> network ไม่ได้เรียนรู้ว่า "เรเดียนคืออะไร" — มันเรียนรู้แค่ว่า "คายเลขแบบไหน
> แล้ว reward สูง" ท่อแปลงค่าทั้งหมดเป็นสิ่งที่**คงที่ตลอดการฝึก** network จึง
> ค่อย ๆ ปรับตัวให้เข้ากับท่อนี้เองผ่าน gradient **หน่วยเป็นเรื่องของมนุษย์ที่
> ออกแบบระบบ ไม่ใช่เรื่องของ network**

#### จุดสำคัญ: policy ไม่เคยสั่ง torque โดยตรง

ขั้น [5] คือ **PD controller** ที่ทำงานอยู่ **นอก** neural network:

$$\boxed{\;\tau = k_p\,(q_{target} - q_{current}) - k_d\,\dot{q}_{current}\;} \tag{9}$$

**ความหมาย:** ยิ่งห่างจากมุมเป้าหมายมาก ยิ่งออกแรงมาก (พจน์ $k_p$) แต่ถ้ากำลัง
ขยับเร็วอยู่ ให้หน่วงไว้ (พจน์ $k_d$) — เป็นตัวควบคุมคลาสสิกที่ไม่มีอะไร
เกี่ยวกับ RL เลย

| | PD ตัวนี้รันอยู่ที่ไหน |
| --- | --- |
| ในซิม | `ImplicitActuatorCfg` ข้างใน PhysX ที่ 200 Hz (4 รอบย่อยต่อ policy step ตาม `decimation=4`) |
| บนหุ่นยนต์จริง | **motor driver board** ที่ความถี่สูง — policy แค่ส่ง $q_{target}$ ไปให้ผ่าน SDK |

นี่คือเหตุผลที่ `03-sf-task-anatomy.md` เขียนว่า *"The policy never emits torque
directly, which is what makes the ONNX portable to hardware"* — ถ้า policy สั่ง
torque ตรง ๆ มันจะต้องรันที่ 200 Hz+ และต้องรู้พลศาสตร์ของมอเตอร์แต่ละตัว แต่การ
สั่งเป็น**ตำแหน่งเป้าหมาย**ทำให้ policy รันแค่ 50 Hz ก็พอ แล้วปล่อยให้ PD
ความถี่สูงบนบอร์ดจัดการรายละเอียดแทน

---

## 2.6 `act` กับ `act_inference` — ไม่มี "transition" ระหว่างสองโหมด

ขั้น [2] ในท่อข้างบนบอกว่า "ตอน deploy ข้ามขั้นนี้" — คำถามที่ตามมาคือ **แล้ว
มันสลับตอนไหน?** คำตอบคือ **ไม่มีการสลับเลย เพราะมันคือคนละโปรแกรมที่รันคนละ
เวลา**

```text
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  09:00   $ python train.py --task Isaac-Limx-SF-Blind-Flat-v0
          ┌────────────────────────────────────────────┐
          │  โปรแกรมที่ 1: train.py                     │
          │  วนลูป 15,000 iterations                    │
          │  ใช้ act()  ← สุ่มทุกครั้ง ตลอดหลายชั่วโมง   │
          │  ปรับ weight ทุก iteration                  │
          └────────────────────────────────────────────┘
  15:00   บันทึก logs/.../model_15000.pt  แล้วโปรแกรมจบ
                              │
                              │  ← มีแค่ "ไฟล์ weight" ที่ข้ามช่วงนี้ไป
                              │     ไม่มีโปรแกรมไหนรันอยู่เลย
                              ▼
  15:30   $ python play.py --task Isaac-Limx-SF-Blind-Flat-Play-v0
          ┌────────────────────────────────────────────┐
          │  โปรแกรมที่ 2: play.py                      │
          │  โหลด model_15000.pt เข้ามา                 │
          │  ใช้ act_inference()  ← ไม่สุ่ม              │
          │  ไม่ปรับ weight เลย (แค่เอามาใช้)            │
          └────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

ดูจากโค้ดจะชัดที่สุด — สองฟังก์ชันนี้อยู่ในคลาสเดียวกัน ใช้ `self.actor`
**ก้อนเดียวกัน weight ชุดเดียวกัน**
([actor_critic.py:159-168](../tron1-rl-isaaclab/rsl_rl/rsl_rl/modules/actor_critic.py#L159-L168)):

```python
def act(self, observations, **kwargs):          # train.py เรียกใช้
    self.update_distribution(observations)      #   สร้าง N(μ, σ) โดย μ = self.actor(obs)
    return self.distribution.sample()           #   สุ่มออกมา

def act_inference(self, observations):          # play.py เรียกใช้
    actions_mean = self.actor(observations)     #   เรียก self.actor(obs) ตรง ๆ
    return actions_mean                         #   คืนค่าเลย ไม่สุ่ม
```

$$\begin{aligned}
\texttt{act}(s) &= \underbrace{\texttt{actor}(s)}_{\mu} + \text{noise}(\sigma) \\[4pt]
\texttt{act\_inference}(s) &= \underbrace{\texttt{actor}(s)}_{\mu}
\end{aligned}$$

**`act_inference` ก็คือ `act` ที่ตัดขั้นสุ่มออก** — ไม่มีการ "เปลี่ยนโหมด" ไม่มี
flag ที่ต้องตั้ง แค่คนละโปรแกรมเรียกคนละฟังก์ชัน

เทียบกับนักดนตรี:

| | ซ้อม (`train.py` → `act`) | แสดงจริง (`play.py` → `act_inference`) |
| --- | --- | --- |
| ใช้ทักษะชุดไหน | ทักษะที่มี ณ ตอนนั้น | **ทักษะชุดเดียวกัน** |
| ทำอะไรเพิ่ม | ลองเล่นเพี้ยนไปนิดหน่อย เพื่อดูว่าแบบไหนเพราะกว่า | เล่นเวอร์ชันที่ดีที่สุดที่รู้ ไม่ทดลอง |
| ทักษะเปลี่ยนไหม | เปลี่ยน (เก่งขึ้นเรื่อย ๆ) | ไม่เปลี่ยน (ใช้อย่างเดียว) |

#### วงจรชีวิตทั้งหมด รวมถึงหุ่นยนต์จริง

```text
[1] train.py        ใช้ act() (สุ่ม)              →  ได้ model_15000.pt
                                                        │
[2] play.py         ใช้ act_inference() (ไม่สุ่ม)   ← โหลดไฟล์นี้
       │                                                ดูผลในซิม + export
       └───────────────────────────────────────────►  ได้ policy.onnx
                                                        │
[3] tron1-rl-deploy  รันบนหุ่นยนต์จริง ไม่สุ่มเช่นกัน  ← โหลดไฟล์นี้
```

ไฟล์ `.onnx` ที่ export ไปลงหุ่นยนต์จริง **บรรจุแต่ actor network เท่านั้น** —
ไม่มี $\sigma$ ไม่มี critic ไม่มีตัวสุ่ม เพราะบนหุ่นยนต์จริงไม่ต้องเรียนรู้อะไร
อีกแล้ว มีแต่การเอา $\mu$ ไปใช้

> **Remark.** ตรงนี้เชื่อมกับที่ `03-sf-task-anatomy.md` บันทึกว่า critic ถูก
> ทิ้งตอน deploy — $\sigma$ ก็ถูกทิ้งด้วยเหตุผลเดียวกัน ทั้งคู่เป็น
> **"เครื่องมือสำหรับการเรียนรู้" ไม่ใช่ "ส่วนหนึ่งของการควบคุม"**
>
> ผลข้างเคียงที่สังเกตได้: หุ่นใน `play.py` จะเดินนิ่งกว่าที่เห็นตอนเทรนเสมอ
> เพราะตัดความสั่นจากการสุ่มออกไปแล้ว

**จบภาค 2 แล้วเรารู้อะไรบ้าง:** policy คือ neural network ที่คาย $\mu$ 8 ตัว;
action จริงคือการสุ่มรอบ ๆ $\mu$; ตัวเลขนั้นเดินทางผ่านท่อ 5 ขั้นจนกลายเป็น
แรงบิดที่มอเตอร์; และ weight ถูกปรับด้วย gradient descent **สิ่งที่ยังไม่รู้คือ
loss ในสมการ (6) หน้าตาเป็นยังไง** — ต้องรู้ก่อนว่าทำไมวิธีตรงไปตรงมาใช้ไม่ได้
(ภาค 3) แล้วค่อยประกอบ PPO ขึ้นมา (ภาค 4)
---
---

# ภาค 3 — ทางตัน: ทำไมวิธี RL คลาสสิกใช้กับ Tron1 ไม่ได้

ทุกวิธีในภาคนี้พยายามแก้สมการ (1) เหมือนกันหมด — แต่ละอันล้มเหลวด้วยเหตุผล
คนละแบบ และความล้มเหลวเหล่านี้แหละที่ชี้ทางไปสู่ PPO ในภาค 4

---

## 3.1 tabular methods (Q-learning ดั้งเดิม, Dynamic Programming)

วิธี RL ยุคแรก ๆ แก้สมการ Bellman (3) ได้ตรง ๆ โดยสร้าง **ตาราง** เก็บค่า $V(s)$
หรือ $Q(s,a)$ ไว้ทุกคู่ state-action แล้วอัปเดตค่าซ้ำ ๆ จนกว่าตัวเลขจะนิ่ง
(converge)

ปัญหาคือ ตารางต้องมี **แถวจำกัด** — ใช้ได้ดีเมื่อ state นับจำนวนได้ เช่น
grid world ที่มีแค่ 100 ช่อง แต่ state ของ Tron1 คือเวกเตอร์ของเลขทศนิยม
(`base_ang_vel` เป็น float 3 ค่า, `joint_pos` เป็น float 8 ค่า ฯลฯ) — เอียง
0.001 องศากับ 0.002 องศา ถือเป็นคนละ state ทางคณิตศาสตร์ ถ้าจะทำตาราง
ต้องมีจำนวนแถวเป็น**อนันต์** ทำไม่ได้จริง

> **Remark.** ปัญหานี้เรียกว่า **continuous state space** (สถานะต่อเนื่อง)
> ตรงข้ามกับ **discrete state space** (สถานะนับได้ เช่น ตำแหน่งบนกระดาน
> หมากรุก) Tron1 มีทั้ง continuous state *และ* continuous action space
> (offset มุมข้อต่อก็เป็น float เช่นกัน) — เป็นโจทย์ที่หนักที่สุดในสองมิตินี้
> พร้อมกัน

ทางแก้คือแทนที่ตารางด้วย**ฟังก์ชันประมาณค่า (function approximator)** — ให้
neural network รับ state เป็น input แล้วทำนาย $V(s)$ หรือ action ที่ดีที่สุด
ออกมาแทน โครงข่ายหนึ่งตัวครอบคลุม state ทุกค่าได้ (ไม่ใช่แค่ค่าที่เคยเจอ)
เพราะมันเรียนรู้ **ความสัมพันธ์** ไม่ใช่ท่องจำเป็นแถว ๆ — นี่คือจุดเริ่มต้นของ
**deep RL** และเป็นเหตุผลที่ policy ของ Tron1 คือโครงข่าย
`42 → 512 → 256 → 128 → 8` (ภาค 2) ไม่ใช่ตาราง

**สรุปสั้น ๆ:** tabular methods ใช้ไม่ได้ เพราะต้องการพื้นที่ state-action
ที่นับจำนวนได้จำกัด แต่ปัญหาหุ่นยนต์เป็น continuous — ต้องเปลี่ยนจากตาราง
เป็น neural network เพื่อ "สรุปความรู้" (generalize) ข้ามค่าที่ไม่เคยเห็นมาก่อน

---

## 3.2 Monte Carlo — และ TD/bootstrapping แก้อะไร

สมมติเราไม่มีสมการ Bellman เลย แล้วอยากรู้ $V(s)$ วิธีตรงไปตรงมาที่สุดคือ:
**ปล่อยให้ episode เล่นจนจบ แล้วนับผลรวม reward จริงที่เกิดขึ้น** เอาไปเป็น
ค่าประมาณของ $V(s)$ — เรียกวิธีนี้ว่า **Monte Carlo (MC) estimation**

ปัญหาของ MC สองข้อ:

1. **ต้องรอ episode จบก่อนถึงจะเรียนรู้ได้อะไรเลย** — Tron1 หนึ่ง episode ยาว
   ถึง 1000 policy steps (20 วินาทีที่ 50Hz ตามหัวข้อ 1.4) ถ้าต้องรอจนจบ
   ทุกครั้งก่อนอัปเดต จะช้ามาก
2. **variance สูง** — ผลรวม reward ตลอด 1000 step ขึ้นอยู่กับความสุ่มของ
   *ทุก* action ตลอดทาง (การสุ่มจาก policy, การสุ่ม domain randomization
   ฯลฯ) ทำให้ตัวเลขที่ได้แกว่งไปมามาก แม้จะเริ่มจาก state เดียวกันสองครั้ง
   ก็อาจได้ผลรวมต่างกันลิบลับ — โมเดลเรียนรู้ได้ช้าเพราะ "สัญญาณ" จมอยู่ใน
   "สัญญาณรบกวน"

ทางแก้คือ **bootstrapping** ที่เห็นในสมการ Bellman (3) แล้ว — แทนที่จะรอ
ผลจริงทั้ง episode เราใช้ **ค่าประมาณ ณ ตอนนี้** ของ state ถัดไปแทน:

$$V(s_t) \approx r_t + \gamma\, V(s_{t+1})$$

วิธีนี้เรียกว่า **Temporal Difference (TD) learning** — เรียนรู้ได้ทันทีจาก
แค่หนึ่ง step ไม่ต้องรอจบ episode และ variance ต่ำกว่ามาก (เพราะพึ่งพาความสุ่ม
แค่ 1 step ไม่ใช่ทั้ง 1000 step) แต่ก็มีข้อเสียของมันเอง: เพราะ $V(s_{t+1})$
เป็นแค่ค่า *ประมาณ* (อาจยังไม่แม่นยำ) การใช้มันมาคำนวณต่อจึงมี **bias**
(อคติ) ติดมาด้วย

| วิธี | ต้องรอ episode จบไหม | Variance | Bias |
| --- | --- | --- | --- |
| Monte Carlo | ต้องรอ | สูง | ไม่มี (ใช้ผลจริง) |
| TD (1 step) | ไม่ต้องรอ | ต่ำ | มี (พึ่งค่าประมาณ) |

นี่คือ **trade-off ระหว่าง bias กับ variance** ที่เป็นหัวใจของ RL เชิงปฏิบัติ
เกือบทั้งหมด — **จำตารางนี้ไว้ให้ดี เพราะ GAE (หัวข้อ 4.4) ถูกออกแบบมาเพื่อ
"เลือกจุดกึ่งกลาง" ระหว่างสองขั้วนี้โดยตรง** แทนที่จะเลือกสุดโต่งฝั่งใดฝั่งหนึ่ง

---

## 3.3 multi-armed bandits

**Bandit problem** คือปัญหาที่ตัดสินใจ action แล้ว "จบ" ทันที — ไม่มีผลต่ออนาคต
เลย (ชื่อมาจากเครื่องสล็อตแมชชีนหลายคัน แต่ละคันมีอัตราจ่ายไม่รู้ค่า จะดึงคันไหน
ให้ได้เงินเยอะสุด) มันคือ MDP ที่ตัด "state ที่เปลี่ยนไปตาม action" ออก เหลือแค่
"เลือก action → ได้ reward → จบ" (ดูตารางในหัวข้อ 1.2)

Tron1 ไม่ใช่แบบนั้นเลย — action ตอนนี้ (มุมข้อต่อ) **ส่งผลโดยตรงต่อ state
ถัดไป** (ท่าทาง, ความเร็ว, การทรงตัว) ซึ่งจะเป็นตัวกำหนดว่า action ต่อไปควรเป็น
อะไร นี่คือ **sequential decision problem** — ปัญหาที่ต้องคิดถึงผลกระทบระยะยาว
ของแต่ละการตัดสินใจ ตรงกับสิ่งที่สมการ (1) เขียนไว้ตั้งแต่ต้น (ผลรวมของ reward
หลาย step ไม่ใช่ reward ก้าวเดียว)

พูดสั้น ๆ คือ bandits ไม่มี "state" ที่มีความหมายเชิงเวลา ในขณะที่หัวใจของ
ปัญหาการเดินของหุ่นยนต์คือ ท่าทางตอนนี้กำหนดว่าจะล้มหรือไม่ล้มในอีกไม่กี่ก้าว
ข้างหน้า — ตัดออกจากการพิจารณาไม่ได้เลย

---

**สรุปภาค 3 — ข้อจำกัดสามข้อที่บังคับรูปร่างของคำตอบ:**

| ทางตัน | บังคับให้ต้องมีอะไร |
| --- | --- |
| tabular ใช้ไม่ได้ (continuous space) | → ต้องใช้ **neural network** เป็น function approximator |
| MC ช้าและ variance สูง / TD มี bias | → ต้องมีวิธี**ผสมสองขั้ว** = GAE (หัวข้อ 4.4) |
| ไม่ใช่ bandit (action ส่งผลต่ออนาคต) | → ต้องมีตัวประเมิน**ผลระยะยาว** = critic (หัวข้อ 4.1) |

สามข้อนี้รวมกันคือโครงกระดูกของ PPO พอดี — ภาค 4 ประกอบมันขึ้นมาทีละชิ้น
---
---

# ภาค 4 — PPO ประกอบขึ้นมายังไง

ภาคนี้ประกอบชิ้นส่วนที่ภาค 3 บอกว่าจำเป็น เข้าด้วยกันทีละชิ้น จนกลายเป็น
อัลกอริทึมที่รันอยู่จริงใน `train.py`

---

## 4.1 Actor-Critic — ทำไมต้องมี network สองก้อน

หัวข้อ 2.1 บอกไปแล้วว่า Tron1 มี actor กับ critic — ตรงนี้คือ**เหตุผลเชิง
อัลกอริทึม**ว่าทำไมถึงต้องเป็นสองก้อน

จนถึงตอนนี้เรามีสองสิ่งที่แยกกันอยู่:

- **Value function $V(s)$** — บอกว่า state นี้ดีแค่ไหนโดยรวม (หัวข้อ 1.6)
- **Policy $\pi(a|s)$** — ตัดสินใจว่าจะทำ action อะไร (ภาค 2)

วิธีคิดแบบแรก ๆ (value-based RL เช่น Q-learning) ฝึกแค่ value function
แล้ว "เลือก action ที่ทำให้ value สูงสุด" — ใช้ได้ดีเมื่อ action มีจำนวนจำกัด
(นับได้) แต่ Tron1 มี action เป็น continuous (offset มุมข้อต่อ float 8 ค่า)
การจะ "ลองทุก action แล้วเลือกตัวที่ดีที่สุด" ทำไม่ได้ เพราะมีอนันต์ทางเลือก

ทางแก้คือฝึก **สองเครือข่ายคู่กัน**:

| ชื่อ | หน้าที่ | ใน Tron1 |
| --- | --- | --- |
| **Actor** ($\pi$) | ตัดสินใจ action โดยตรงจาก state — คือ policy ตัวจริงที่ deploy ไปยังหุ่นยนต์ | โครงข่าย `42 → 512 → 256 → 128 → 8` |
| **Critic** ($V$) | ประเมินว่า state ที่ actor เจอนั้นดีแค่ไหน ใช้เป็น "ครู" ช่วยสอน actor | โครงข่าย `230 → 512 → 256 → 128 → 1` |

Actor เรียนรู้จาก **policy gradient** — ปรับน้ำหนักของโครงข่ายไปในทิศทางที่
ทำให้ action ที่เคย "ได้ผลดีกว่าที่คาดไว้" มีโอกาสถูกเลือกมากขึ้นในอนาคต
ส่วน critic คอยบอกว่า "ดีกว่าที่คาดไว้" แปลว่าอะไร — นี่คือที่มาของคำว่า
**advantage** (หัวข้อ 4.4)

> **Remark.** รายละเอียดว่า actor เห็น 36 มิติ ส่วน critic เห็น 227 มิติ
> (**asymmetric actor-critic**) อยู่ในตารางหัวข้อ 2.1 แล้ว — สรุปสั้น ๆ คือ
> critic โกงดูข้อมูลที่มีแค่ในซิมได้ เพราะมันถูกทิ้งตอน deploy ส่วน actor
> ต้องเก่งจากสิ่งที่เซนเซอร์จริงวัดได้เท่านั้น

---

## 4.2 Critic ต่างจาก Reward function ยังไง

สองอย่างนี้สับสนกันง่ายมาก เพราะทั้งคู่คายตัวเลขที่ "บอกว่าดีแค่ไหน" ออกมา
เหมือนกัน — แต่อยู่คนละฝั่งของปัญหาเลย

**Reward function คือ *โจทย์* — Critic คือ *เครื่องมือแก้โจทย์***

| | **Reward function** | **Critic** |
| --- | --- | --- |
| ใครสร้าง | **มนุษย์เขียน** (วิศวกร LimX) | **เรียนรู้เอง**จากข้อมูล |
| เป็นอะไร | โค้ด Python ที่คำนวณตามสูตรตายตัว | neural network `230 → 512 → 256 → 128 → 1` |
| อยู่ตรงไหนใน MDP | **ช่อง $R$** ในแบบฟอร์ม 5 ช่อง (หัวข้อ 1.2) | **ไม่อยู่ในแบบฟอร์มเลย** — เป็นส่วนของอัลกอริทึม |
| ตอบคำถามอะไร | "step ที่เพิ่งผ่านไป ได้กี่คะแนน" | "จากตรงนี้ไปจนจบ น่าจะได้อีกกี่คะแนน" |
| มองไปทางไหน | **อดีต** (สิ่งที่เพิ่งเกิด) | **อนาคต** (สิ่งที่ยังไม่เกิด) |
| เปลี่ยนระหว่างเทรนไหม | **ไม่เปลี่ยนเลย** ตายตัวตั้งแต่ต้นจนจบ | **เปลี่ยนทุก iteration** (20 gradient updates — หัวข้อ 4.6) |
| ผิดได้ไหม | ผิดไม่ได้ — มันคือ*นิยาม*ของคำว่า "ดี" | **ผิดได้ตลอด** เป็นแค่การทำนาย |
| ตอน deploy | ไม่ใช้ (ไม่มีใครให้คะแนนหุ่นจริง) | ถูกทิ้ง |
| อยู่ไฟล์ไหน | `mdp/rewards.py` + `RewardsCfg` | `rsl_rl/modules/actor_critic.py` |

**ดูที่จังหวะเดียวกันจะเห็นชัด** — สมมติหุ่นอยู่ที่ step 300 กำลังเดินอยู่ดี ๆ:

```text
                   ┌─ Reward function พูดว่า ─────────────────┐
                   │  "step นี้ทรงตัวดี +0.8                   │
step 300 ────────► │   เท้ากระแทกแรงไปหน่อย −0.1               │
                   │   รวม step นี้ได้ 0.7"    ← ข้อเท็จจริง    │
                   └───────────────────────────────────────────┘

                   ┌─ Critic พูดว่า ──────────────────────────┐
                   │  "ท่าทางแบบนี้ ดูแล้วน่าจะเก็บได้อีกราว    │
                   │   650 แต้มจนจบ episode"   ← การทำนาย      │
                   └───────────────────────────────────────────┘
```

**Reward = ข้อเท็จจริงที่วัดได้แล้ว** (คำนวณจากฟิสิกส์ที่เพิ่งเกิด ไม่มีทางผิด)
**Critic = การเดาอนาคต** (อาจเดาผิดเป็น 400 ก็ได้ ถ้าจริง ๆ อีก 3 step หุ่นจะล้ม)

#### ความสัมพันธ์: critic เรียนรู้ *จาก* reward

สองอย่างนี้ไม่ได้แยกกันอยู่ — **reward คือ "ครู" ของ critic**

$$\underbrace{V(s)}_{\text{critic ทำนาย}} \;\longleftarrow\; \text{เรียนรู้ให้เข้าใกล้} \;\longrightarrow\; \underbrace{\mathbb{E}\Big[\textstyle\sum_k \gamma^k \, r_{t+k}\Big]}_{\text{ผลรวม reward จริงในอนาคต}}$$

```text
reward function (มนุษย์เขียน)
      │  เป็นเป้าหมายให้เรียนรู้ (value loss)
      ▼
   critic (เรียนรู้ที่จะทำนายผลรวมของ reward)
      │  ให้ค่า V(s) ไปคำนวณ advantage
      ▼
   actor (เรียนรู้จาก advantage)
```

#### เทียบกับ LQR — สำหรับคนที่มาจากสาย control

ถ้าเคยผ่าน optimal control มาแล้ว ความสัมพันธ์นี้คุ้นมาก:

| Optimal control (LQR) | RL (PPO) |
| --- | --- |
| **stage cost** $x^TQx + u^TRu$ — เลือก $Q, R$ เอง | **reward function** — วิศวกรเขียนเอง |
| **cost-to-go** $V(x) = x^TPx$ — ได้จากแก้สมการ Riccati | **critic** $V(s)$ — ได้จากการฝึก neural network |
| $P$ คำนวณได้ตรง ๆ เพราะระบบเป็นเชิงเส้น | หาตรง ๆ ไม่ได้ เพราะระบบไม่เชิงเส้นและไม่รู้สมการ → **ต้องประมาณด้วย NN** |

**critic คือ cost-to-go function ที่หาด้วยวิธีวิเคราะห์ไม่ได้ เลยต้องเดาเอาจาก
ข้อมูล** — แนวคิดเดียวกันเป๊ะกับ $P$ ใน Riccati equation ต่างกันแค่วิธีได้มา

#### ผลที่ตามมาในทางปฏิบัติ — สำคัญที่สุดในหัวข้อนี้

**critic แก้ reward function ที่ผิดไม่ได้**

ถ้า reward function เขียนไว้ผิด (เช่น ให้คะแนนความเร็วสูงมากจนหุ่นเรียนรู้ที่จะ
กระโดดไปข้างหน้าแล้วล้ม) critic ก็จะเรียนรู้ที่จะ **ทำนายคะแนนของพฤติกรรมผิด ๆ
นั้นได้อย่างแม่นยำ** — มันไม่มีทางรู้ว่าเป้าหมายที่ตั้งไว้ผิด

นี่คือเหตุผลที่ **งานวิศวกรรมส่วนใหญ่ของ RL อยู่ที่ reward function ไม่ใช่ที่
อัลกอริทึม** — Tron1 มี reward ถึง 20 terms พร้อมน้ำหนักที่จูนมาอย่างละเอียด
(`03-sf-task-anatomy.md`) ในขณะที่ critic เป็นแค่ MLP ธรรมดาที่ไม่มีอะไรพิเศษเลย

> เชื่อมกับตารางใน `04-file-structure-vs-ros2.md`: การเปลี่ยน "เป้าหมาย" ทำที่
> `cfg/SF/limx_base_env_cfg.py` → `RewardsCfg` ส่วนการเปลี่ยน "วิธีเรียนรู้" ทำ
> ที่ `rsl_rl/` — คนละไฟล์ คนละความรับผิดชอบ

---

## 4.3 Encoder กับ "latent" — ซ่อมปัญหาไม่ Markov

หัวข้อ 1.3 ทิ้งหนี้ไว้ว่า Tron1 ไม่ Markov แท้ และหัวข้อ 2.2 พูดถึง "latent
3 ค่าจาก encoder" ลอย ๆ — ตรงนี้คือการชำระหนี้ทั้งสองอย่าง

**ปัญหาที่ encoder ถูกสร้างมาแก้:** เซนเซอร์จริงบนหุ่นยนต์ (IMU + joint
encoder) **วัดความเร็วเชิงเส้นของลำตัว (base linear velocity) ไม่ได้โดยตรง** —
วัดได้แค่ความเร็วเชิงมุม (`base_ang_vel`), แรงโน้มถ่วงที่ฉายลงมา
(`proj_gravity`), มุม/ความเร็วข้อต่อ แต่ "ตอนนี้ลำตัวกำลังเคลื่อนที่ไปข้างหน้า
เร็วแค่ไหน" ไม่มีเซนเซอร์ตัวไหนบอกตรง ๆ แต่ policy จำเป็นต้องรู้ค่านี้ทางอ้อม
เพื่อควบคุมความเร็วการเดินให้แม่นยำ

**ทางแก้:** ถ้าเราดู**ประวัติ**ของสิ่งที่วัดได้ย้อนหลังไปหลาย step (เช่น
ขาไหนกำลังเคลื่อนไหวแบบไหน ต่อเนื่องกันมา 10 step) จริง ๆ แล้วสามารถ
"เดา" ความเร็วลำตัวได้ในระดับหนึ่ง — คล้ายกับที่คนเราหลับตาแล้วยังรู้สึกได้
คร่าว ๆ ว่ากำลังเดินเร็วแค่ไหน จากจังหวะการก้าวขา ไม่ใช่จากการมองเห็น

**encoder** คือ neural network **อีกก้อนหนึ่ง แยกจาก actor และ critic** ที่ทำ
หน้าที่นี้โดยเฉพาะ:

```text
obsHistory (ประวัติ observation ย้อนหลัง 10 step, แต่ละ step มี 36 ค่า)
  = เวกเตอร์ยาว 360 ตัวเลข (10 × 36)
        │
        ▼
   encoder network:  360 → 256 → 128 → 3   (ก้อนแยกจาก actor)
        │
        ▼
   latent (3 ตัวเลข) — ค่าที่ไม่มีความหมายตรงตัวเป็นภาษามนุษย์ แต่เดาได้ว่า
   น่าจะเป็นการประมาณ base linear velocity โดยอ้อม (สังเกตจากปัญหาที่มันถูก
   สร้างมาแก้)
```

| สัญลักษณ์/คำ | ความหมาย |
| --- | --- |
| **obsHistory** | ประวัติของ observation หลัก (36 มิติ) ย้อนหลัง 10 step ล่าสุด เอามาเรียงต่อกันเป็นเวกเตอร์ยาว 360 |
| **encoder** | neural network ขนาด `360 → 256 → 128 → 3` ที่รับ obsHistory เป็น input — โครงสร้างข้างในเหมือน node/layer ในหัวข้อ 2.2 ทุกประการ เพียงแต่เป็นคนละก้อนที่มีหน้าที่ต่างจาก actor |
| **latent** | output 3 ตัวเลขของ encoder — เรียกว่า "latent" (แปลตรงตัวว่า "แฝงอยู่") เพราะมันเป็นตัวแทนข้อมูลที่ neural network สรุปมาให้เอง ไม่ใช่ค่าที่มนุษย์นิยามไว้ล่วงหน้าเหมือน `joint_pos` หรือ `base_ang_vel` |

จากนั้น latent 3 ค่านี้ถูก**ต่อท้าย (concatenate)** เข้ากับ observation
หลัก 36 ค่า กลายเป็น input ก้อนเดียวที่ actor เห็น — นี่คือที่มาของตัวเลข
"42" ที่พูดถึงตลอดทั้งโน้ตนี้ (3 + 36 + 3 — ดูหัวข้อ 2.2 และโน้ต 06 §1)

> **Remark.** ทำไม encoder ถึงเป็น neural network แยกก้อน ไม่ใช่ชั้นแรกของ
> actor เอง? เพราะมันฝึกด้วยเป้าหมายคนละแบบ — `EncoderCfg.output_detach=True`
> ที่ `04-file-structure-vs-ros2.md` บันทึกไว้ หมายความว่า gradient จาก policy
> loss **ไม่ไหลย้อนกลับเข้าไปปรับ weight ของ encoder** ตัด "สาย" ระหว่าง
> สองก้อนนี้ออกจากกัน encoder ถูกฝึกแยกต่างหากด้วย optimizer ของตัวเอง
> (`extra_optimizer` ใน `ppo.py`) โดยมีเป้าหมายของตัวเอง — ทายค่าจริงที่ critic
> เห็นได้ เช่น `base_lin_vel` จาก privileged observation (หัวข้อ 2.1)
>
> เป็นเทคนิคที่เรียกว่า **auxiliary task / state estimation** ซึ่งแยกออกไปจาก
> PPO core ที่โน้ตนี้โฟกัสอยู่ — รู้แค่ว่ามันคือ "ตัวเดาความเร็วลำตัว" ก็พอ
> สำหรับเข้าใจภาพรวม

> **ทางเลือกอื่นที่ใช้ได้จริงเหมือนกัน.** ปัญหา "ประมาณความเร็วลำตัวจาก IMU +
> joint encoder" เป็นโจทย์คลาสสิกของ **sensor fusion** — หุ่นยนต์สี่ขา/สองขา
> จำนวนมาก (MIT Cheetah, ANYmal รุ่นแรก ๆ) ใช้ **Extended Kalman Filter** ทำ
> งานนี้แทน โดยผสม IMU + forward kinematics + ข้อสมมติว่าเท้าที่แตะพื้นไม่ลื่น
>
> Tron1 เลือก learned encoder แทน ด้วยเหตุผลหลักว่า **ในซิมมี ground truth ให้
> ฝึกฟรี** (physics engine รู้ `base_lin_vel` จริงอยู่แล้ว) จึงไม่ต้อง derive
> dynamics model หรือ tune noise covariance เอง และมันทนต่อ domain
> randomization แบบเดียวกับที่ policy เจอโดยอัตโนมัติ — แลกกับการที่
> interpretability ต่ำกว่า KF (ไม่มี covariance matrix ให้ดูว่า "ตอนนี้ไม่มั่นใจ")
> ทั้งสองวิธีถูกต้องและใช้จริงในวงการ ไม่ใช่ว่าอันใดอันหนึ่งผิด
---

## 4.4 Advantage และ GAE — ให้เครดิต action ยังไงให้แม่นและนิ่ง

**Advantage** $A(s,a)$ นิยามว่า "action นี้ดีกว่าค่าเฉลี่ยที่ policy คาดหวังไว้
ที่ state นี้แค่ไหน":

$$A(s_t, a_t) = Q(s_t,a_t) - V(s_t) \tag{10}$$

ถ้า $A > 0$ แปลว่า action ที่เพิ่งทำดีกว่าค่าเฉลี่ย → policy gradient จะดัน
ให้ actor เลือก action แบบนี้บ่อยขึ้น ถ้า $A < 0$ ก็ดันให้เลือกน้อยลง

> **จุดที่ควรสังเกต:** สิ่งที่ขับเคลื่อนการเรียนรู้ไม่ใช่ reward ดิบ แต่เป็น
> **advantage** — action ที่ได้ reward 0.5 อาจถือว่า "แย่" ถ้า critic คาดไว้ว่า
> ควรได้ 0.8 และ action ที่ได้ reward ติดลบก็อาจถือว่า "ดี" ถ้าสถานการณ์นั้น
> แย่อยู่แล้วและ critic คาดว่าจะแย่กว่านั้นอีก

ปัญหาคือจะประมาณ $A$ ยังไง — ตรงนี้แหละที่ **bias-variance trade-off จากหัวข้อ
3.2 กลับมามีบทบาท** เราสามารถประมาณ $A$ แบบ Monte Carlo ล้วน ๆ (variance สูง,
bias ต่ำ) หรือแบบ TD 1-step ล้วน ๆ (variance ต่ำ, bias สูง) ก็ได้ —
**Generalized Advantage Estimation (GAE)** คือสูตรที่ผสมทั้งสองขั้วเข้าด้วยกัน
แบบถ่วงน้ำหนัก โดยมีพารามิเตอร์ $\lambda$ (lambda) ควบคุมสัดส่วน:

$$A^{GAE}_t = \sum_{k=0}^{\infty} (\gamma\lambda)^k\, \delta_{t+k}, \qquad \delta_t = r_t + \gamma V(s_{t+1}) - V(s_t) \tag{11}$$

| ขั้นตอน | เหตุผล |
| --- | --- |
| $\delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)$ | TD-error หนึ่งก้าว — ส่วนต่างระหว่าง "สิ่งที่เกิดขึ้นจริง" กับ "สิ่งที่ critic คาดไว้" |
| $A^{GAE}_t = \delta_t + \gamma\lambda\, \delta_{t+1} + (\gamma\lambda)^2 \delta_{t+2} + \dots$ | รวม TD-error หลาย step เข้าด้วยกัน ถ่วงน้ำหนักลดลงเรื่อย ๆ ตามระยะเวลา |

**ความหมาย:** ถ้า $\lambda = 0$ สูตรนี้จะเหลือแค่ $\delta_t$ (TD 1-step ล้วน ๆ
— bias สูง variance ต่ำ) ถ้า $\lambda = 1$ จะกลายเป็น Monte Carlo ล้วน ๆ
(variance สูง bias ต่ำ) ค่ากึ่งกลางอย่าง `lam = 0.95` ที่ Tron1 ใช้คือการเลือก
จุดที่ "เชื่อ critic พอสมควร แต่ก็ยังฟังผลจริงในระยะสั้น ๆ ถัดไปด้วย" — จุดที่
ในทางปฏิบัติมักให้การเรียนรู้ที่เร็วและนิ่งกว่าสุดโต่งทั้งสองฝั่ง

---

## 4.5 PPO — ทำไมต้อง "clip" การอัปเดต

Policy gradient ธรรมดา (แบบ REINFORCE ดั้งเดิม) มีปัญหาสำคัญ: ถ้าอัปเดต
น้ำหนักของ actor แรงเกินไปในการอัปเดตครั้งเดียว policy ใหม่อาจ "กระโดด" ไป
ไกลจาก policy เดิมมากจนพฤติกรรมพังไปเลย (เช่น หุ่นยนต์ที่กำลังเริ่มเดินได้
จู่ ๆ ล้มทุกครั้ง) และเพราะ action ถัดไปถูกเลือกจาก policy ที่พังแล้ว
ข้อมูลที่เก็บมาต่อจากนี้ก็ใช้ไม่ได้ต่อเนื่องกันไปอีก — วนเป็นปัญหาลูกโซ่

**Proximal Policy Optimization (PPO)** แก้ปัญหานี้ด้วยการ**จำกัดขนาดของการ
เปลี่ยนแปลง policy ในแต่ละรอบอัปเดต** ไม่ให้ policy ใหม่ต่างจาก policy เดิม
มากเกินไป มีสามกลไกที่ Tron1 ใช้ร่วมกัน:

### (ก) Clipped surrogate objective

แทนที่จะปล่อยให้ advantage ผลักดันการอัปเดตแบบไม่มีขีดจำกัด PPO ตัด (clip)
สัดส่วนความน่าจะเป็นของ action ระหว่าง policy ใหม่กับเก่าไม่ให้ห่างเกินช่วง
$[1-\epsilon, 1+\epsilon]$:

$$L^{CLIP} = \mathbb{E}\Big[\min\big(\rho_t A_t,\; \text{clip}(\rho_t, 1-\epsilon, 1+\epsilon)\, A_t\big)\Big], \qquad \rho_t = \frac{\pi_{new}(a_t|s_t)}{\pi_{old}(a_t|s_t)} \tag{12}$$

`clip 0.2` ในตาราง PPO hyperparameters ของ Tron1 คือค่า $\epsilon = 0.2$ นี่เอง
— แปลว่าความน่าจะเป็นของ action ใหม่ ห้ามต่างจาก policy เดิมเกิน ±20% ต่อ
การอัปเดตหนึ่งรอบ

#### $\rho$ ถูกคำนวณและใช้ตอนไหน — ไล่ตามโค้ดจริง

$\rho$ ทำงานใน **ช่วง C (update)** ของ iteration (ดูกายวิภาคในหัวข้อ 4.6) แต่
วัตถุดิบของมันถูกเก็บไว้ตั้งแต่ **ช่วง A (collection)**

**ตอนช่วง A — เก็บ "ค่าอ้างอิงจากอดีต" ไว้ก่อน**
([ppo.py:147](../tron1-rl-isaaclab/rsl_rl/rsl_rl/algorithm/ppo.py#L147)):

```python
self.transition.actions_log_prob = self.actor_critic.get_actions_log_prob(actions)
```

พอสุ่มได้ $a = 0.61$ ก็คำนวณ $\pi_{old}(0.61|s) = 1.088$ ด้วยสมการ (7)
**เก็บใส่ buffer ไว้พร้อมกับ $a$ เอง**

**ตอนช่วง C — ถามคำถามเดิมกับ policy ที่เปลี่ยนไปแล้ว**
([ppo.py:212](../tron1-rl-isaaclab/rsl_rl/rsl_rl/algorithm/ppo.py#L212)):

```python
actions_log_prob_batch = self.actor_critic.get_actions_log_prob(actions_batch)
```

หยิบ $a = 0.61$ **ตัวเดิม** ออกมาจาก buffer แล้วถามใหม่ว่า *"policy ที่อัปเดต
ไปแล้ว จะเลือก $a$ ตัวนี้ด้วยความหนาแน่นเท่าไหร่?"* — สมมติ gradient ดัน $\mu$
จาก 0.42 → 0.60 จะได้ $\pi_{new}(0.61|s) = 1.329$

**คำนวณ $\rho$** ([ppo.py:252-253](../tron1-rl-isaaclab/rsl_rl/rsl_rl/algorithm/ppo.py#L252-L253)):

```python
ratio = torch.exp(actions_log_prob_batch - torch.squeeze(old_actions_log_prob_batch))
```

(ลบกันใน log space แล้ว `exp` = การหารในปริภูมิปกติ)

$$\rho = \frac{\pi_{new}(0.61|s)}{\pi_{old}(0.61|s)} = \frac{1.329}{1.088} = 1.22$$

**$\rho$ บอกว่า "policy ใหม่อยากทำ action นี้ มากขึ้นหรือน้อยลงกว่าเดิมกี่เท่า":**

| $\rho$ | ความหมาย |
| --- | --- |
| $1.0$ | policy ไม่เปลี่ยนเลยสำหรับ action นี้ |
| $1.22$ | policy ใหม่อยากทำ action นี้ **มากขึ้น 22%** |
| $0.7$ | policy ใหม่อยากทำ action นี้ **น้อยลง 30%** |

**เอาไปคูณกับ advantage**
([ppo.py:256-260](../tron1-rl-isaaclab/rsl_rl/rsl_rl/algorithm/ppo.py#L256-L260)):

```python
surrogate         = -advantages_batch * ratio
surrogate_clipped = -advantages_batch * torch.clamp(ratio, 1.0 - 0.2, 1.0 + 0.2)
surrogate_loss    = torch.max(surrogate, surrogate_clipped).mean()
```

ในตัวอย่างนี้ $\rho = 1.22$ **เกิน $1+\epsilon = 1.2$** → ถูก `clamp` ลงเหลือ
1.2 → **PPO หยุดให้รางวัลกับการดัน $\mu$ ไปไกลกว่านี้ในรอบเดียว**

> นี่คือคำตอบว่าทำไมต้องเก็บ `logprob` ไว้ตั้งแต่ช่วง A — **เพราะต้องมี "ค่า
> อ้างอิงจากอดีต" ไว้เทียบว่า policy เคลื่อนไปเท่าไหร่แล้ว** ถ้าไม่เก็บ ก็วัด
> ไม่ได้ และนี่ก็เป็นเหตุผลเดียวกับที่ข้อมูลชุดเดิมถูกใช้ซ้ำได้ 5 epochs
> (หัวข้อ 4.6) — ทุก epoch $\pi_{new}$ เคลื่อนออกจาก $\pi_{old}$ ไปอีก
> clipping คือสิ่งที่คอยจำกัดไม่ให้เคลื่อนไกลเกินจนข้อมูลเก่าใช้ไม่ได้

### (ข) KL-adaptive learning rate

นอกจาก clip แล้ว Tron1 ยังวัด **KL divergence** — ตัวเลขที่บอกว่า policy ใหม่
"ต่างจาก" policy เก่ามากแค่ไหนโดยรวม (ไม่ใช่แค่ต่อ action เดียว) — แล้วปรับ
learning rate $\alpha$ ในสมการ (6) เองอัตโนมัติให้ KL เข้าใกล้เป้าหมาย
`desired_kl = 0.01`:

- ถ้า KL วัดได้ **สูงกว่าเป้า** (policy เปลี่ยนเร็วเกินไป) → **ลด** learning rate
- ถ้า KL วัดได้ **ต่ำกว่าเป้า** (เปลี่ยนช้าเกินไป) → **เพิ่ม** learning rate

นี่คือเหตุผลที่ log ของ Tron1 แสดง learning rate เริ่มจาก `0.0000` ตามที่โน้ต
`03-sf-task-anatomy.md` บันทึกไว้: มันเริ่มจาก `lr 1e-3` ที่ตั้งไว้ แล้วปรับ
ลดลงอัตโนมัติในช่วงแรกจนกว่า KL จะเข้าใกล้เป้า

### (ค) Entropy bonus

ปัญหาสุดท้ายคือ policy อาจ "มั่นใจเกินไป" เร็วเกินไป — เลือก action เดิมซ้ำ ๆ
ก่อนที่จะสำรวจพอ (ปัญหา exploration vs exploitation จากหัวข้อ 2.4) PPO บวก
**entropy** (ตัววัดความสุ่ม/ความไม่แน่นอนของการแจกแจง action — ในกรณี Gaussian
คือฟังก์ชันของ $\sigma$) เข้าไปใน objective เป็นโบนัส เพื่อสนับสนุนให้ policy
ยังคง "สำรวจ" ต่อไปสักพัก ไม่รีบฟันธง — `entropy_coef 0.01` คือน้ำหนักของโบนัส
นี้ (เล็กน้อย เพราะเป็นแค่ตัวช่วยไม่ใช่เป้าหมายหลัก)

**นี่คือกลไกที่ต้าน $\sigma$ ไม่ให้หดจนเหลือศูนย์เร็วเกินไป** — กราฟ
`Mean action noise std` ที่ค่อย ๆ ลดลง (หัวข้อ 2.4) คือผลของการชักเย่อระหว่าง
surrogate loss (อยากให้ $\sigma$ เล็ก) กับ entropy bonus (อยากให้ $\sigma$ ใหญ่)

### สรุปเป็นภาพเดียว

**PPO** = policy gradient (หัวข้อ 4.1) + advantage จาก GAE (หัวข้อ 4.4)
+ กลไกป้องกันไม่ให้อัปเดตแรงเกินไป (clip + KL-adaptive LR)
+ กลไกกันไม่ให้หยุดสำรวจเร็วเกินไป (entropy bonus)

— ทุกชิ้นส่วนที่พูดมาทั้งหมดในโน้ตนี้ ประกอบกันเป็นบรรทัดใน
`agents/limx_rsl_rl_ppo_cfg.py`

---

## 4.6 หน่วยของฝั่งอัลกอริทึม: iteration และ batch

หัวข้อ 1.4 แนะนำ **step** กับ **episode** ไปแล้ว — สองอันนั้นเป็นหน่วยของ
*ปัญหา* ตรงนี้คือหน่วยของ *การเรียนรู้* ซึ่งเพิ่งจะอธิบายได้ตอนนี้ เพราะต้อง
รู้จัก GAE กับ clip ก่อน

#### สามหน่วยเทียบกัน

**iteration** คือ **หนึ่งรอบเต็มของวงจร "ออกไปเก็บประสบการณ์ แล้วกลับมาเรียนรู้
จากมัน"**

| | **step** | **episode** | **iteration** |
| --- | --- | --- | --- |
| คือหน่วยของอะไร | การตัดสินใจ 1 ครั้ง | การเล่น 1 รอบ ของหุ่น 1 ตัว | **1 รอบของ เก็บข้อมูล → เรียนรู้** |
| ขนาดใน Tron1 | 0.02 วินาที | สูงสุด 1000 steps | 24 steps × 2048 envs |
| จบเมื่อไหร่ | ทุก 0.02s | ล้ม หรือครบ 20 วินาที | เก็บครบ 24 steps ต่อ env แล้วอัปเดตเสร็จ |
| เกี่ยวกับหุ่นกี่ตัว | 1 ตัว | 1 ตัว | **ทั้ง 2048 ตัวพร้อมกัน** |
| weight เปลี่ยนไหม | ไม่ | ไม่ (แต่เปลี่ยนระหว่างทาง!) | **เปลี่ยน 20 ครั้ง** (ดูกายวิภาคด้านล่าง) |
| เห็นในล็อกยังไง | ไม่แสดง | `Mean episode length` | `Learning iteration 137/15000` |

อีกคำที่เจอบ่อยคือ **batch** (ในโค้ดเรียก rollout) — คือ**ก้อนข้อมูล**ที่เก็บได้
ในหนึ่ง iteration: `num_envs × num_steps_per_env` = 2048 × 24 = **49,152
transitions** (แต่ละ transition คือ `state, action, reward, next_state`)

จุดสำคัญคือ **batch ไม่ใช่ episode** และ **หนึ่ง batch ไม่ต้องรอ episode จบ** —
Isaac Lab รัน 2048 หุ่นยนต์พร้อมกันแบบขนาน แต่ละตัวเดินไปแค่ 24 steps แล้วหยุด
เก็บข้อมูล ไม่ว่า episode ของมันจะจบหรือยัง

> **ทำไมไม่รอ episode จบก่อนค่อยอัปเดต?** เพราะ episode ยาวถึง 1000 steps
> ถ้ารอจบก่อนทุกครั้งจะช้ามาก — **นี่คือปัญหาแบบ Monte Carlo ในหัวข้อ 3.2
> นั่นเอง** การตัดเป็น batch สั้น ๆ ทำให้อัปเดตบ่อยขึ้นมาก episode ที่ยังไม่จบ
> ตอนตัด batch จะถูก "ตัดกลางคัน" แล้ว critic ช่วยทำนายส่วนที่เหลือแทนด้วย
> bootstrapping — เชื่อมตรงกับ GAE ในหัวข้อ 4.4

#### กายวิภาคของ 1 iteration — ข้างในเกิดอะไรขึ้นบ้าง

หนึ่ง iteration ไม่ใช่การอัปเดตครั้งเดียว แต่แบ่งเป็น **4 ช่วงที่ทำต่อกัน**
(ไล่ตามโค้ดใน
[on_policy_runner.py:175-240](../tron1-rl-isaaclab/rsl_rl/rsl_rl/runner/on_policy_runner.py#L175-L240)
และ [ppo.py:185-290](../tron1-rl-isaaclab/rsl_rl/rsl_rl/algorithm/ppo.py#L185-L290)):

```text
┌─ ช่วง A: เก็บข้อมูล (collection) ─────────── ไม่แตะ weight เลย ──┐
│  วน 24 รอบ:                                                      │
│     obs จาก 2048 envs                                            │
│       → actor คำนวณ μ, สุ่มได้ a          (act — หัวข้อ 2.4)     │
│       → critic คำนวณ V(s)                  ← เก็บไว้ใช้ช่วง B     │
│       → env.step(a)  ฟิสิกส์ขยับ → reward, obs ใหม่, done         │
│       → เก็บ (obs, a, reward, done, V, logprob) ลง buffer         │
│  ผลลัพธ์: 24 × 2048 = 49,152 transitions ใน buffer                │
└───────────────────────────────────────────────────────────────────┘
                              ↓
┌─ ช่วง B: คำนวณ advantage (compute_returns) ─────────────────────┐
│  bootstrap: ให้ critic ทำนาย V ที่ step สุดท้าย (เพราะ rollout    │
│             ถูกตัดกลางคัน ยังไม่จบ episode)                       │
│  แล้วไล่ย้อนจาก step 24 → step 1 คำนวณ δ และ A^GAE (สมการ 11)    │
└───────────────────────────────────────────────────────────────────┘
                              ↓
┌─ ช่วง C: อัปเดต weight (update) ─────── ตรงนี้แหละที่เรียนรู้ ───┐
│  วน 5 epochs × 4 minibatches = 20 รอบ:                           │
│     หยิบ minibatch (12,288 ตัวอย่าง)                             │
│       → คำนวณ loss (clipped surrogate + value loss + entropy)     │
│       → optimizer.zero_grad()                                     │
│       → loss.backward()      ← backprop หา gradient ทุก weight    │
│       → optimizer.step()     ← ทำสมการ (6) ปรับ weight จริง       │
│  (encoder มี optimizer แยกของตัวเอง วนอีก 20 รอบเช่นกัน)          │
└───────────────────────────────────────────────────────────────────┘
                              ↓
┌─ ช่วง D: บันทึกผล ────────────────────────────────────────────────┐
│  พิมพ์ล็อก + เขียน tensorboard                                    │
│  ทุก ๆ 500 iterations (save_interval) บันทึก model_<it>.pt        │
└───────────────────────────────────────────────────────────────────┘
```

**จุดที่มักเข้าใจผิด: `1 iteration ≠ 1 gradient update`**

`num_learning_epochs = 5` × `num_mini_batches = 4` = **20 ครั้งที่
`optimizer.step()` ถูกเรียกต่อหนึ่ง iteration** — ข้อมูลชุดเดียวกัน (49,152
transitions) ถูกใช้ซ้ำ 5 รอบ โดยแต่ละรอบหั่นเป็น 4 ก้อนย่อย

> **ทำไมใช้ข้อมูลซ้ำได้ 5 รอบ ทั้งที่ PPO เป็น on-policy?** — นี่คือเหตุผล
> ที่ PPO ต้องมี clipping ตั้งแต่แรก (หัวข้อ 4.5) พอ epoch ที่ 2, 3, 4, 5
> policy ได้เปลี่ยนไปจากตอนที่เก็บข้อมูลมาแล้ว ข้อมูลจึง "เก่า" ลงเรื่อย ๆ
> — `clip 0.2` และ KL-adaptive LR คือกลไกที่จำกัดไม่ให้ policy เคลื่อนไกลจน
> ข้อมูลเก่าใช้ไม่ได้ **ถ้าไม่มี clipping จะใช้ซ้ำได้แค่ 1 epoch เท่านั้น** —
> การใช้ซ้ำได้ 5 รอบคือสิ่งที่ทำให้ PPO ประหยัดข้อมูลกว่า policy gradient
> รุ่นก่อนหน้ามาก

**เวลาที่ใช้ในแต่ละช่วง** — `train.py` พิมพ์ให้ดูทุก iteration ในรูปแบบ
([on_policy_runner.py:321](../tron1-rl-isaaclab/rsl_rl/rsl_rl/runner/on_policy_runner.py#L321)):

```text
Computation: 41523 steps/s (collection: 0.420s, learning 0.180s)
                            └─ ช่วง A ─┘        └─ ช่วง B+C ─┘
```

และบันทึกลง tensorboard แยกเป็น `Perf/collection time` กับ `Perf/learning_time`

ปกติช่วง A จะกินเวลามากกว่า เพราะต้องรันฟิสิกส์จริง ๆ — นี่คือเหตุผลที่การ
เพิ่ม `--num_envs` ช่วยให้เรียนรู้เร็วขึ้นต่อ iteration (ได้ข้อมูลเยอะขึ้นโดย
เวลาช่วง A เพิ่มไม่มากนัก เพราะ GPU ทำขนานอยู่แล้ว) จนกว่าจะชน VRAM

> **สิ่งที่ควรจดตอนรัน `exp_001`.** `exp_001_baseline_sf.md` มีช่อง "steps/s
> reported at iteration 100" รออยู่ — ตัวเลข `steps/s` ในบรรทัดนี้แหละคือค่าที่
> ต้องกรอก และการดูสัดส่วน collection ต่อ learning จะบอกได้ว่าคอขวดอยู่ที่
> ฟิสิกส์ (เพิ่ม envs ไม่ช่วยแล้ว) หรือที่การอัปเดต network

#### `episode` กับ `iteration` ไม่ซ้อนกัน

`step` ซ้อนอยู่ใน `episode` และซ้อนอยู่ใน `iteration` ด้วย แต่สองอันหลัง
**เป็นการ "หั่น" เส้นเวลาเดียวกันคนละแบบ ที่ตัดกันไปมา**:

```text
เส้นเวลาของหุ่นยนต์ตัวหนึ่ง (แต่ละ ▪ คือ 1 step):

step:       ▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪▪ ...

iteration:  └─24─┘└─24─┘└─24─┘└─24─┘└─24─┘└─24─┘└─24─┘└─24─┘ ...
             #1    #2    #3    #4    #5    #6    #7    #8

episode:    └──────────────── 1000 steps ─────────────────────┘
                          (episode เดียว)
```

**หนึ่ง episode กินเวลาประมาณ $1000 \div 24 \approx 42$ iterations** — แปลว่า
**ระหว่างที่หุ่นยนต์ตัวหนึ่งเดินอยู่ใน episode เดียว weight ของมันถูกอัปเดตไป
แล้วราว 42 ครั้ง** หุ่นตัวนั้นจึง "เปลี่ยนสมอง" 42 รอบระหว่างที่ยังเดินรอบเดียว
— ฟังดูแปลกแต่ไม่มีปัญหา เพราะแต่ละครั้งเปลี่ยนนิดเดียว (มี `clip 0.2` กับ
KL-adaptive LR คุมอยู่)

> นี่คือคำตอบเชิงตัวเลขของเรื่อง "เห็นหุ่นค่อย ๆ ตั้งได้ระหว่างเทรน" ในหัวข้อ
> 2.4 — weight ถูกอัปเดตทุก ~0.5 วินาทีของเวลาจำลอง ไม่ใช่ทุก episode

**ภาพรวมทั้งการเทรน:**

| | จำนวน |
| --- | --- |
| 1 iteration | 24 × 2048 = **49,152 transitions** |
| 15,000 iterations (`max_iterations`) | **737,280,000 transitions** (~740M) |
| หุ่น 1 ตัวได้ประสบการณ์ | 15,000 × 24 = 360,000 steps = **2 ชั่วโมง** ของเวลาจำลอง |
| รวมทั้ง 2048 ตัว | 2 ชม. × 2048 ≈ **170 วัน** ของประสบการณ์การเดิน |

ตัวเลข ~740M ตรงกับที่ `exp_001_baseline_sf.md` ประเมินไว้พอดี — และแถวสุดท้าย
คือเหตุผลทั้งหมดที่ต้องเทรนในซิม: เก็บประสบการณ์ 170 วันภายในไม่กี่ชั่วโมง
โดยไม่มีหุ่นยนต์จริงพังสักตัว

**สรุปว่าแต่ละคำตอบคำถามอะไร:**

- **step** = หน่วยเวลาที่เล็กที่สุด (policy ตัดสินใจ 1 ครั้ง)
- **episode** = หน่วยของ "การลองหนึ่งรอบ" → ตอบว่า *หุ่นเก่งแค่ไหน*
  (`Mean episode length` ยิ่งยาวยิ่งดี)
- **iteration** = หน่วยของ "การเรียนรู้หนึ่งครั้ง" → ตอบว่า *ฝึกไปแล้วเท่าไหร่*
  (`Learning iteration 137/15000`)

#### ทำไม 1 iteration ถึงเก็บ 24 steps — ไม่ใช่ 4 หรือ 200

ค่านี้มาจาก `num_steps_per_env = 24` ใน
[limx_rsl_rl_ppo_cfg.py:11](../tron1-rl-isaaclab/exts/bipedal_locomotion/bipedal_locomotion/tasks/locomotion/agents/limx_rsl_rl_ppo_cfg.py#L11)
— **เป็น hyperparameter เหมือน 512/256/128 และ `scale=0.25`** คือไม่มีสูตร
คำนวณออกมาตรง ๆ แต่มี **ข้อจำกัดจริงบีบจากทั้งสองด้าน**:

| ถ้าน้อยเกินไป (เช่น 4) | ถ้ามากเกินไป (เช่น 200) |
| --- | --- |
| GAE มองอนาคตได้สั้นเกินไป → advantage พึ่ง critic เกือบทั้งหมด → **bias สูง** | ข้อมูลช่วงต้น rollout "เก่า" — เก็บด้วย policy เวอร์ชันก่อน แต่เอาไปอัปเดต policy ปัจจุบัน (**PPO เป็น on-policy**) |
| อัปเดตถี่แต่แต่ละครั้งข้อมูลน้อย → gradient แกว่ง | เปลืองแรม — buffer โตตามตรง |
| ค่าใช้จ่ายคงที่ต่อการอัปเดต (backprop, optimizer) มาถี่เกินไป | feedback ช้า — จำนวนการอัปเดตต่อเวลาลดลง |

**เหตุผลเชิงปริมาณที่ดีที่สุด: ต้องยาวพอคลุม "ระยะมองเห็น" ของ GAE**

GAE (สมการ 11) ถ่วงน้ำหนัก TD-error ที่ไกลออกไปด้วย $(\gamma\lambda)^k$ ซึ่ง
ลดลงแบบเอ็กซ์โพเนนเชียล ด้วยค่าที่ Tron1 ใช้:

$$\gamma\lambda = 0.99 \times 0.95 = 0.9405$$

**ระยะมองเห็นที่แท้จริง (effective horizon)** ของการถ่วงน้ำหนักแบบนี้คือ:

$$\frac{1}{1-\gamma\lambda} = \frac{1}{0.0595} \approx 17 \text{ steps}$$

แปลว่า GAE "สนใจ" ผลที่เกิดในอีกราว 17 steps ข้างหน้าเป็นหลัก — **ถ้าเก็บ
rollout สั้นกว่านั้น เท่ากับตัดข้อมูลที่ GAE ยังต้องใช้ทิ้ง** advantage จะพึ่ง
bootstrap จาก critic มากเกินไป (bias สูง ตามตารางในหัวข้อ 3.2)

**24 > 17 → มีระยะเผื่อพอดี** ที่ 24 steps น้ำหนักเหลือ $0.9405^{24} \approx 0.23$
คือลดเหลือ ~23% แล้ว ส่วนที่เกินจากนั้นให้ critic ทำนายต่อด้วย bootstrap

**ข้อจำกัดเรื่อง VRAM — เกี่ยวกับเครื่องนี้โดยตรง**

rollout buffer ต้องเก็บทุก transition ไว้ในหน่วยความจำ GPU:

```text
ต่อ 1 transition: policy obs 42 + critic obs 227 + obsHistory 360
                  + action 8 + value + logprob + μ 8 + σ 8 + ...
                ≈ 660 floats × 4 bytes ≈ 2.6 KB
```

| `num_steps_per_env` | ขนาด buffer (ที่ 2048 envs) |
| --- | --- |
| **24 (ปัจจุบัน)** | **~130 MB** |
| 100 | ~540 MB |
| 200 | ~1.1 GB |

README บันทึกว่าการ์ด 12 GB ของเครื่องนี้อยู่ต่ำกว่าที่ Isaac Lab แนะนำ (16 GB)
อยู่แล้ว — การเพิ่ม `num_steps_per_env` กิน VRAM เพิ่มแบบเป็นเส้นตรง

**ขนาด batch ที่ได้ออกมาก็ต้องพอ**

```text
49,152 transitions  ÷  num_mini_batches 4   =  12,288 ต่อ minibatch
                    ×  num_learning_epochs 5  →  ข้อมูลแต่ละชิ้นถูกใช้ 5 รอบ
```

12,288 ตัวอย่างต่อ gradient step ถือว่าเยอะมากสำหรับงานแบบนี้ gradient จึงนิ่ง
**จำนวน env ที่มาก (2048) ชดเชยความสั้นของ rollout (24) ได้** — ถ้าใช้แค่ 16
envs ตอน smoke test จะได้แค่ 384 transitions ต่อ iteration ซึ่งน้อยเกินจะเรียนรู้
จริง (นั่นคือเหตุผลที่ smoke test ใน README ตั้ง `--max_iterations 2` แค่เช็กว่า
รันผ่าน ไม่ได้หวังผลการเรียนรู้)

**สรุป: 24 ไม่ได้มาจากการคำนวณ แต่มาจากการอยู่ในช่วงที่เงื่อนไขทุกข้อพอใจ
พร้อมกัน:**

- ยาวกว่าระยะมองเห็นของ GAE (~17 steps)
- สั้นพอที่ข้อมูลยังไม่เก่าเกินไปสำหรับ on-policy
- buffer ไม่เกิน VRAM
- คูณกับ 2048 envs แล้วได้ batch ใหญ่พอให้ gradient นิ่ง

ค่าในช่วง 16-32 เป็นค่ามาตรฐานที่ใช้กันทั่วไปในงาน legged locomotion บน
Isaac Lab / legged_gym ไม่ใช่ตัวเลขที่ LimX คิดขึ้นเอง

> **ถ้าจะทดลองเปลี่ยน:** แก้ที่ `agents/limx_rsl_rl_ppo_cfg.py` ตามตารางใน
> `04-file-structure-vs-ros2.md` — แต่ถ้าเปลี่ยน `num_steps_per_env` ควร
> พิจารณา `lam` ควบคู่ไปด้วย เพราะทั้งคู่กำหนดระยะมองเห็นร่วมกัน
---
---

# ภาค 5 — สรุปและเชื่อมกลับเข้ากับ config จริง

## 5.1 ตาราง hyperparameter — แต่ละค่ามาจากหัวข้อไหน

| Hyperparameter | ค่าที่ใช้ | คืออะไร | อธิบายที่หัวข้อ |
| --- | --- | --- | --- |
| `gamma` | `0.99` | discount factor — อนาคตสำคัญเท่าปัจจุบันแค่ไหน | 1.5 (สมการ 1), 1.6 (สมการ 2) |
| `lr` | `1e-3` | learning rate $\alpha$ — ก้าวยาวแค่ไหนต่อ gradient step | 2.2 (สมการ 6) |
| `lam` | `0.95` | $\lambda$ ใน GAE — จุดกึ่งกลาง bias/variance | 4.4 (สมการ 11) |
| `clip` | `0.2` | $\epsilon$ ใน clipped objective | 4.5 (สมการ 12) |
| `desired_kl` | `0.01` | เป้าหมายของ KL-adaptive learning rate | 4.5 (ข) |
| `entropy_coef` | `0.01` | น้ำหนัก entropy bonus — กันไม่ให้ $\sigma$ หดเร็วเกินไป | 4.5 (ค) |
| `num_steps_per_env` | `24` | ความยาว rollout ต่อ env ต่อ iteration | 4.6 |
| `num_learning_epochs` | `5` | ใช้ข้อมูลชุดเดิมซ้ำกี่รอบ | 4.6 |
| `num_mini_batches` | `4` | หั่น batch เป็นกี่ก้อนย่อยต่อ epoch | 4.6 |
| `max_iterations` | `15000` | ฝึกกี่ iteration | 4.6 |
| hidden dims | `512, 256, 128` | ขนาดชั้นกลางของ actor/critic | 2.2 |
| `scale` (action) | `0.25` | ตัวแปลง action ไร้หน่วย → เรเดียน | 2.5 |
| `stiffness` / `damping` | `45.0` / `1.5`, `0.8` | $k_p$, $k_d$ ของ PD controller | 2.5 (สมการ 9) |

## 5.2 ดัชนีสมการทั้งหมด

| สมการ | ชื่อ | หัวข้อ |
| --- | --- | --- |
| (1) | Objective — โจทย์ทั้งหมดของ RL | 1.5 |
| (2) | Value function | 1.6 |
| (3) | สมการ Bellman | 1.6 |
| (4) | $V(s) = \mathbb{E}[G_t]$ — value คือค่าคาดหวังของ return | 1.7 |
| (5) | Node output ของ neural network | 2.2 |
| (6) | Gradient descent update | 2.2 |
| (7) | Gaussian PDF — คำนวณ $\pi(a\|s)$ | 2.3 |
| (8) | การสุ่ม $a = \mu + \sigma\varepsilon$ | 2.4 |
| (9) | PD controller | 2.5 |
| (10) | Advantage | 4.4 |
| (11) | GAE | 4.4 |
| (12) | PPO clipped surrogate objective | 4.5 |

## 5.3 สิ่งที่ยังไม่ได้พูดถึง

- **Domain randomization** — ไม่ใช่ส่วนหนึ่งของ PPO เอง แต่เป็นเทคนิคแยก
  ต่างหากที่ทำให้ policy ที่ฝึกในซิมูเลเตอร์ใช้งานได้บนหุ่นยนต์จริง
  (sim-to-real) — เห็นได้ใน `EventsCfg` ตามที่ `03-sf-task-anatomy.md`
  บันทึกไว้ พารามิเตอร์ที่ถูกสุ่มเหล่านี้คือสิ่งที่ critic เห็นใน 227 มิติ
  (หัวข้อ 2.1)
- **Gait command** — `gait_phase` 2 มิติ กับ `gait_command` 4 มิติ ใน
  observation มาจาก `mdp/commands/gait_command.py` ซึ่งเป็น term ที่ LimX เขียน
  เพิ่มเอง จังหวะการเดินของ Tron1 จึงเป็นสิ่งที่**ถูกสั่ง** ไม่ใช่สิ่งที่
  **เกิดขึ้นเอง**จากการเรียนรู้
- **รายละเอียดของ 20 reward terms** — อยู่ใน `03-sf-task-anatomy.md` พร้อม
  น้ำหนักครบทุกตัว โน้ตนี้อ้างถึงแต่ไม่ได้แจกแจงซ้ำ
- **การฝึก encoder จริง ๆ** — หัวข้อ 4.3 บอกว่ามันฝึกแยกด้วย `extra_optimizer`
  แต่ไม่ได้ลงลึกว่า loss ของมันหน้าตายังไง เป็นเรื่องของ auxiliary task /
  state estimation ที่แยกออกไปจาก PPO core

## 5.4 เส้นทางอ่านต่อ

| อยากรู้อะไร | ไปที่ไหน |
| --- | --- |
| ตัวเลข config ของ SF task ครบ ๆ (obs/action/reward/randomisation) | `03-sf-task-anatomy.md` |
| จะแก้อะไรต้องไปแก้ไฟล์ไหน | `04-file-structure-vs-ros2.md` |
| repo ไหนทำอะไรในระบบนิเวศ TRON1 | `01-repo-map.md` |
| ทำไม deploy ต้องผ่าน SDK ไม่ใช่เขียน `mj_data.ctrl` ตรง ๆ | `02-sdk-vs-sim.md` |
| จะรัน baseline ยังไง ต้องจดอะไรบ้าง | `experiments/exp_001_baseline_sf.md` |
| อ่าน PPO paper ต้นฉบับ | Schulman et al. 2017 |

---

*แหล่งอ้างอิงแนวคิด: Sutton & Barto, "Reinforcement Learning: An Introduction"
(บทที่ 3–4 สำหรับ MDP/Bellman, บทท้าย ๆ สำหรับ policy gradient); Schulman et
al. 2017, "Proximal Policy Optimization Algorithms"; Schulman et al. 2015,
"High-Dimensional Continuous Control Using Generalized Advantage Estimation".
ตัวเลข config และเลขบรรทัดโค้ดทั้งหมดตรวจกับ repo จริงใน
`tron1-rl-isaaclab/` และ `IsaacLab/` ของเครื่องนี้ — ลิงก์โค้ดเป็น relative
path จึงใช้ได้เฉพาะในเครื่องที่ clone ทั้งสอง repo ไว้แล้ว (ทั้งคู่อยู่ใน
`.gitignore` ตาม README).*
