# notes/

Understanding, not instructions. Setup lives in the top-level `README.md`.

| File | Contents |
|---|---|
| [01-repo-map.md](01-repo-map.md) | The 12 TRON1 repos at `github.com/limxdynamics` and how they stack |
| [02-sdk-vs-sim.md](02-sdk-vs-sim.md) | What `limxsdk-lowlevel` is, and why the simulator impersonates the robot over a socket |
| [03-sf-task-anatomy.md](03-sf-task-anatomy.md) | `Isaac-Limx-SF-Blind-Flat-v0` pulled apart: obs/action/reward/randomisation/PPO |
| [04-file-structure-vs-ros2.md](04-file-structure-vs-ros2.md) | Isaac Sim vs Isaac Lab vs project, mapped onto ROS 2 habits, and where to change what |
| [00-glossary.md](00-glossary.md) | The single glossary for notes 05–07: every abbreviation, symbol, unit, config constant and equation number defined **once**, each with its object type (function / distribution / vector / scalar / …), the `file.py:NN` where it lives, and its value in the one running toy example (2 env × 4 steps) that all three notes compute on. Read first; the notes link here instead of re-defining terms |
| [05-rl-fundamentals-ground-up.md](05-rl-fundamentals-ground-up.md) | RL from zero, in Thai: MDP as a 5-slot form, Markov property and why violating it breaks everything, what the policy network physically is (nodes, layers, ELU, why no output activation), the full action pipeline μ → sample → ×0.25 → PD → torque, `act` vs `act_inference`, then the dead ends (tabular, MC vs TD, on-policy + importance sampling) and how PPO is assembled — policy gradient, actor-critic, encoder, GAE, the five-part loss, the five units (step/episode/iteration/epoch/mini-batch) and why 24 steps per rollout — with the global equations (1)–(34) boxed where they are derived, every term typed and tied to real config values and line numbers, and every number taken from the running 2 × 4 toy |
| [06-training-walkthrough-bridge.md](06-training-walkthrough-bridge.md) | Bridge between theory (05), the toy iteration (07) and code lines: resolves the 42 = 3+36+3 actor-input question, a step→section→equation→file:line table for one full iteration, an 8-rule guide to reading RL notation (symbols themselves live in the glossary), §3.4 the code-level mechanics of one weight update — ℓ vs `.grad` as two boxes, one full step on the toy's e0 t0 cell, equations (35) ∂L/∂ℓ, (36) Adam, (37) the KL-adaptive lr rule and its cumulative-drift behaviour — §3.5's value chain E1–E15 mapping every formula to its exact code line with shapes, worked on an 8-D widening of the same toy cell plus a self-check script, and an errata section for the source walkthrough in `sources/` |
| [07-compute-at-every-level.md](07-compute-at-every-level.md) | Hand-derivable walkthrough of what is computed at each unit — step, episode, iteration, epoch, mini-batch — on a 2 env × 4 step × 1-D toy so every number fits on a whiteboard: derives log π from the Gaussian PDF, proves GAE recursion = closed-form (with the `done` mask cutting the sum), derives ∂L/∂μ and shows why it moves μ toward good actions, computes when clipping actually fires in all four sign cases from one twin pair of table cells, entropy and KL for Gaussians, all eight cells across both mini-batches (so ρ < 1 appears too), the time-out bootstrap the toy cannot show; ends with a toy→Tron1 scaling table (with the source walkthrough's Tron1-scale magnitudes labelled as illustrative) and a one-page formula sheet that indexes the shared equation numbers by level |
| [sources/](sources/) | External source material, kept for reference and **not** part of the note sequence: `training_numerical_walkthrough.md` is the Gemini-generated walkthrough that notes 05–07 mined; its header lists the errors found in it (parameter counts, invented numbers) so it is not read as ground truth |
| [diagrams/](diagrams/) | Six self-contained HTML diagrams (open in a browser): `tron1-actor-critic.html` — Sutton & Barto agent–environment layout with every arrow a symbol from note 05; `tron1-ppo-iteration.html` — one PPO iteration as an Algorithm-1 flowchart with the loop-back that defines "iteration"; `tron1-value-flow.html` — the 15-equation value chain E1→E15 as a vertical ladder, every arrow labelled with the shape flowing through it, marking the two points where the data type changes (tensor→distribution, 8 dims→1 scalar); `tron1-deploy.html` — the same agent–environment picture after training stops: critic, GAE and PPO ghosted out because they are not on the robot, with the coral arrow showing that the advantage was spent into θ and never computed again; `tron1-five-units.html` — the 24 × 2048 rollout buffer as one table, showing step / episode / mini-batch / epoch / iteration as five ways of slicing the same data. `tron1-policy-objects.html` — the two steps from policy to action, with dashed zones marking what kind of object each stretch is (data tensor, learned NN parameters, a throwaway `Normal` object, results): every box is an object, every arrow is a line of code. All symbols are real LaTeX, typeset by MathJax (vendored offline at `diagrams/mathjax/tex-svg.js`) — so `\(A_t=\sum_k(\gamma\lambda)^k\delta_{t+k}\)` in the source renders as the equation you would write on paper |
