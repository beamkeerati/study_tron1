# notes/

Understanding, not instructions. Setup lives in the top-level `README.md`.

| File | Contents |
|---|---|
| [01-repo-map.md](01-repo-map.md) | The 12 TRON1 repos at `github.com/limxdynamics` and how they stack |
| [02-sdk-vs-sim.md](02-sdk-vs-sim.md) | What `limxsdk-lowlevel` is, and why the simulator impersonates the robot over a socket |
| [03-sf-task-anatomy.md](03-sf-task-anatomy.md) | `Isaac-Limx-SF-Blind-Flat-v0` pulled apart: obs/action/reward/randomisation/PPO |
| [04-file-structure-vs-ros2.md](04-file-structure-vs-ros2.md) | Isaac Sim vs Isaac Lab vs project, mapped onto ROS 2 habits, and where to change what |
| [05-rl-fundamentals-ground-up.md](05-rl-fundamentals-ground-up.md) | RL from zero, in Thai: MDP as a 5-slot form, Markov property and why violating it breaks everything, what the policy network physically is (nodes, layers, why no output activation), the full action pipeline μ → sample → ×0.25 → PD → torque, `act` vs `act_inference`, step/episode/iteration/batch, why 24 steps per iteration, then Bellman → MC/TD → actor-critic → GAE → PPO — every term tied back to real config values and line numbers |
| [06-training-walkthrough-bridge.md](06-training-walkthrough-bridge.md) | Bridge between theory (05), the numerical walkthrough, and code lines: resolves the 42 = 3+36+3 actor-input question, a step→section→equation→file:line table for one full iteration, an 8-rule guide to reading RL notation intuitively with a full symbol table, §3.5's equation-by-equation value chain (E1–E15) mapping every formula to its exact code line with shapes and a worked numeric pass plus a self-check script, and fidelity notes on where the walkthrough's numbers are wrong (parameter counts) |
| [07-compute-at-every-level.md](07-compute-at-every-level.md) | Hand-derivable walkthrough of what is computed at each unit — step, episode, iteration, epoch, mini-batch — on a 2 env × 4 step × 1-D toy so every number fits on a whiteboard: derives log π from the Gaussian PDF, proves GAE recursion = closed-form (with the `done` mask cutting the sum), derives ∂L/∂μ and shows why it moves μ toward good actions, computes when clipping actually fires (two scenarios), entropy and KL for Gaussians; ends with a toy→Tron1 scaling table and a one-page formula sheet |
| [diagrams/](diagrams/) | Three self-contained HTML diagrams (open in a browser): `tron1-actor-critic.html` — Sutton & Barto agent–environment layout with every arrow a symbol from note 05; `tron1-ppo-iteration.html` — one PPO iteration as an Algorithm-1 flowchart with the loop-back that defines "iteration"; `tron1-value-flow.html` — the 15-equation value chain E1→E15 as a vertical ladder, every arrow labelled with the shape flowing through it, marking the two points where the data type changes (tensor→distribution, 8 dims→1 scalar); `tron1-deploy.html` — the same agent–environment picture after training stops: critic, GAE and PPO ghosted out because they are not on the robot, with the coral arrow showing that the advantage was spent into θ and never computed again; `tron1-five-units.html` — the 24 × 2048 rollout buffer as one table, showing step / episode / mini-batch / epoch / iteration as five ways of slicing the same data. All symbols are real LaTeX, typeset by MathJax (vendored offline at `diagrams/mathjax/tex-svg.js`) — so `\(A_t=\sum_k(\gamma\lambda)^k\delta_{t+k}\)` in the source renders as the equation you would write on paper |
